# Favoreco 画面情報設計

> 2026-07-13確定。画面デザインを作る前の情報階層・画面責務の正本。
> Mystoriumの見た目は流用せず、閲覧/編集分離、対象/体験回分離、Draft保存方式だけを参照する。
> 2026-07-21更新: 下部タブの`記録`は`FAVO`へ変更し、推し専用ではなく人物・場所・作品・体験を含む「自分の好き」から横断して振り返るページとする。本文中の旧`記録`タブ表記より本更新を優先する。

## 1. 基本原則

1. 閲覧画面に編集コントロールを混在させない。閲覧画面には編集への入口だけを置く。
2. `ExperienceEvent`（作品・公演・本・施設などの対象）と `Visit`（その日に体験した記録）を画面上でも分離する。
3. `Plan`（予定）、`TicketAttempt`（申込1件）、`Visit`（体験済み記録）を同じ入力フォームで同格に扱わない。
4. 一覧/サマリーでは厳選情報、詳細では保存情報を原則すべて表示する。
5. 登録/編集はDraftへ入力し、保存時だけモデルへ反映する。キャンセルでは正本を変更しない。
6. 長い入力は必須情報を常設し、補助情報をアコーディオンへ分ける。

### アイコン体系

- 下部タブ、共通ヘッダー、ジャンル入口・管理、ジャンルトップ、詳細、一覧、登録フォーム、FAVO、カレンダー、統計、設定で使う意味アイコンは、単色の`Phosphor Light`を第一選択とする。
- 固定名・動的名とも`FavorecoIcon`または`FavorecoIconLabel`を通し、検索0件・未登録等の空状態は`FavorecoContentUnavailableView`を使う。既存コードが持つSF Symbol名からPhosphor字形へ解決し、対応がない記号だけSF Symbolsへ自動フォールバックして、画面ごとに別系統のアイコン素材を増やさない。
- 標準`TabView`のアイコンはPhosphor字形をテンプレート画像化して渡し、選択色・非選択色は標準タブバーへ委ねる。文字グリフViewを`tabItem`へ直接渡さない。
- テーマパークはCastle Turret、生き物はPaw Printを使い、美術・博物はPalette、本はBooks、チケットはTicket、通知はBellを基準とする。
- 記録詳細Heroの日付、時刻、タグ、会場、座席、天気も同じLightアイコンを使う。評価の塗り星・半星、選択状態、表示形式切替、戻る・進む・開閉、Apple固有操作、バッジ差そのものが意味を持つ未対応記号はSF Symbolsを維持する。
- 記録詳細のチケット・座席カードは、状態=Ticket、申込=Identification Card、購入元=Compass、座席=Chair、画像OCR=Scan、URL=Link、手入力=Plusを使う。複数チケットでも同じ割り当てを使う。
- 保存済みのジャンル記号は変更せず、表示時の変換で統一する。ライト／ダークとも塗り分けや多色化をせず、各画面の`foregroundStyle`をそのまま適用する。

### 対応OS方針

- 最低対応OSはiOS 18とし、保存・登録・閲覧・バックアップなど主要機能を利用可能にする。
- 主対象とUI品質基準はiOS 26以上とする。設計をiOS 18の見た目や操作へ固定せず、iOS 26で利用できる新しいSwiftUIのナビゲーション、スクロール、視覚効果、アクセシビリティ、パフォーマンス改善を優先する。
- iOS 26専用APIは`#available`で分岐し、iOS 18では情報や主要操作を失わない簡潔なフォールバックを提供する。
- 実機の最終デザイン確認はiOS 26を優先し、iOS 18は互換性、レイアウト崩れ、起動、保存データの確認を中心にする。

## 2. 情報階層

```text
Home（全ジャンル横断）
├─ ジャンルトップ
│  ├─ 対象詳細（ExperienceEvent）
│  │  ├─ 記録詳細（Visit）
│  │  └─ 予定詳細（Plan）
│  │     └─ 申込詳細（TicketAttempt）
│  └─ ジャンル内記録
└─ 全記録一覧
   └─ 記録詳細（Visit）
```

## 3. 画面責務

| 画面 | 主役 | 役割 |
|---|---|---|
| Home | 全体の今・思い出 | 全ジャンルの予定、期限、最近の写真、ジャンル入口を横断表示 |
| ジャンルトップ | 対象ライブラリ | そのジャンルで登録済みの作品・施設・銘柄などを探す |
| 記録一覧 | Visitの時系列 | 全ジャンルまたは選択ジャンルの体験履歴を探す |
| 対象詳細 | ExperienceEvent | 作品・公演・本・施設など、何についての記録かを表示 |
| 記録詳細 | Visit | その日に体験した内容をすべて閲覧 |
| 予定詳細 | Plan | これから参加する日時・会場と申込状況を確認 |
| 登録 | 新しいVisit | 必須情報を短く入力し、必要な補足だけ展開 |
| 編集 | 対象/記録/予定のいずれか | 1画面で1種類の正本だけを変更 |

記録一覧の検索欄直下には、`すべて`と記録が存在するジャンルを並べた横スクロール式チェックボックスを置く。初期状態は`すべて`を選択し、ジャンルを押すと`すべて`を解除してそのジャンルだけへ絞り込む。その後は複数ジャンルを同時選択でき、最後の選択を外した場合は`すべて`へ戻る。その下中央に`詳細検索`ボタンを置き、右上のフィルターアイコンは廃止する。詳細検索はNavigationStack内の独立ページとして開き、`日付・期間 / タグ / 同行者 / 写真有無 / 並び順`を表示する。タグと同行者は記録に保存済みの値だけを2列チェックボックスで提示し、複数選択できる。同一項目内はOR、期間・タグ・同行者・写真・ジャンル間はANDで絞り込む。期間は`すべて / 今月 / 今年 / 期間指定`から選び、期間指定時だけ開始日と終了日を表示する。開始日・終了日は両日を含み、開始日を終了日より後へ変更した場合は終了日も同日に揃える。条件クリアではジャンルを含むすべての条件を初期状態へ戻す。

上部の自由検索はタイトル・シリーズ・会場・メモだけでなく、キャスト、監督、主催、作者などの人物/団体名と役割、タグ、同行者、OCR文字、自由追加項目、座席、場所名・住所を横断する。空白区切りの複数語はAND条件とし、各語が異なる保存項目に存在しても一致扱いにする。

記録一覧の件数右へ、ジャンルトップと同じ3列ギャラリー、2列ミニ詳細、横長バナーの表示切替を置く。3列は保存した対象アイキャッチ比率の画像と`日付｜評価`の1行情報帯、2列は画像・2行タイトル・日付・5段階評価、横長は画像・ジャンル別の体験済み状態・2行タイトル・日付・会場を表示する。一覧画像には各記録へ追加した写真を自動採用せず、対象アイキャッチだけを非同期縮小表示する。対象アイキャッチがない場合はジャンル色＋アイコンではなく、ジャンル別の既定写真を表示する。画像は各対象のアイキャッチ比率を維持した中央aspect fillとし、映画・観劇のポスター、書影、写真系ジャンルを同じ基準で扱う。初期表示は3列9件、2列6件、横長5件とし、`さらにn件`を押すたび同数ずつ追加する。選択した表示は次回も維持し、旧3列/4列/横長の保存値は対応する新表示へ引き継ぐ。予定・チケット入口は記録画面から外し、カレンダー上部のチケットアイコンへ集約する。

## 4. Home

Home / 記録 / カレンダー / 統計のルート画面は、ステータスバー直下に共通の固定ヘッダーを置く。左の画面名は1行で全体を見せ、Homeのブランド名は先頭を大文字にした `Favoreco` とし、34ptのCormorant Garamond Boldで表示する。ブランド色はライト時にアプリアイコン中央のFに合わせた青墨色 `#192735`、ダーク時は可読性を保つ明るい青灰色とする。ほかの画面名は30ptを基準とする。右は22ptの通知ベルと38ptのプロフィールの順とする。2操作はそれぞれ44ptの押下領域を持つが、ピル、カード、外側の共通背景で囲わない。ヘッダーは安全領域内の基準位置から6pt上へ寄せ、下端にテーマ色を薄くした全画面幅1ptの区切り線を置く。iOS 26のToolbar自動装飾でダークモードに黒いコンテナが出るのを避けるため、ルート画面のヘッダーは通常のHStackで描画する。詳細画面の戻るナビゲーションは従来どおりシステムのNavigationBarを使う。

表示順:

1. 共通ヘッダー: Favoreco、通知、プロフィール
2. ジャンル入口: 共通ヘッダー直下のテキストピルによるコンパクトな`1段横スクロール`を基本とする
3. `Ticket Schedule`: 全ジャンルのチケット取得に向けた次の対応を表示。0件ならセクション自体を隠す
4. `PICK UP`: `気になる / 予定 / 記録`をタブ一体型の1パネルへ統合
5. `FAVORECO REPORT`: `今月 / 今年`をタブで切り替え、写真コラージュと期間集計を表示

2026-07-27以降、Ticket Scheduleの残日数は70pt欄の中で桁数別に組む。1〜2桁は`あと`11pt・日数40pt・`日`11pt、3桁は9pt・26pt・9pt、4桁以上は8pt・21pt・8ptとする。日数と単位は横方向を固定して省略記号へ変えない。この指定は後述する旧固定値`あと`14pt・日数38pt・`日`15ptより優先する。

次工程の期限が未入力のカードは、左欄の`期限を入力`だけを赤系の注意色で表示する。参加日未定時の`日程を入力`はエラーではないため通常の補助色を維持する。

Homeの`Ticket Schedule`カード右情報欄は、公演タイトルを基準位置から1pt下へ補正する。参加日時は12pt Semibold、カレンダーは日時確定／参加日未定とも同じPhosphor図形の13ptへ揃え、未定時は図形を変えず文字とアイコンをオレンジで示す。カード高、タイトルと参加日時の3pt間隔、進捗管理シートとチケット管理一覧の状態アイコンは変更しない。

チケット管理は入口ジャンルから独立したライト配色境界とする。総合Home、観劇・LIVE等のダーク系ジャンルトップ、カレンダーのどこから開いても、Navigation Bar、検索欄、状態・ジャンルフィルター、Section背景、一覧カードを同じライト配色で表示する。親画面のダークEnvironmentは管理画面内へ持ち込まず、追加・編集等の遷移先は各画面自身の配色仕様へ従う。

`PICK UP`は初期タブを`予定`とし、最後に選んだタブを端末へ保存する。各タブ内は全ジャンル横断の最新候補を最大10件だけ、全幅ページングで1件ずつ左右スワイプ表示する。`気になる`はExperienceEventの更新日と未整理Inboxの作成日を混ぜて新しい順、`予定`は参加日時が確定した未終了Planと今日以降のVisitを日時の近い順、`記録`は参加済みVisitを記録日の新しい順とする。同じ対象に未来Planがある場合は`予定`を優先し、`気になる`へ重複表示しない。未確定日Ticketは`予定`へ入れずTicket Scheduleへ残す。タブ切替はタップ、カード切替は左右スワイプとし、カード末尾から別タブや別ジャンルへ連続移動させない。ページ位置は`現在 / 件数`（最大`10 / 10`）を表示し、VoiceOverでは`最新N件中n件目`と読む。0件は空状態、1件では`1 / 1`を表示する。タブ列と本文は同じ外枠と背景を共有し、カードをさらに独立した外枠で囲わない。項目がある場合は3タブ・全ジャンルとも左を132pt幅、間隔12pt、右を残り幅とし、カード内側余白を2ptへ揃える。PICK UP内のカード高は208ptとし、左列は上174ptをアイキャッチ、下30ptをページ位置へ使う。右欄の末尾には4ptの安全域を予約し、操作ボタンの枠線を横ページングのクリップ境界へ重ねない。ページ位置はアイキャッチ幅の中央、右側の操作ボタンと同じ行へ置き、独立したフッターは作らない。PICK UPには気になる項目の追加ボタンを置かない。元画像の縦横比は変形させず固定枠内でfit/fillする。

2026-07-27以降、PICK UPの固定画像枠は全ジャンル・全タブでaspect fitだけを使う。元画像の上下左右を切らず全体を表示し、余った領域はジャンル色の弱い背景として残す。予定詳細／記録詳細ボタンは固定110ptではなく右欄の残り幅へ縮め、編集64pt＋間隔6ptと合わせてカード内へ必ず収める。通常カードと詳細画面の画像規則は変更しない。

`FAVORECO REPORT`は`今月 / 今年`のタブ列と本文を同じ外枠へ統合し、現在の月・年に含まれる記録だけを写真コラージュ、記録数、ジャンル数、写真つき件数、最多ジャンルで要約する。期間切替はタップだけとし、左右スワイプはジャンル移動へ渡す。`今月`は無料範囲を想定し、`今年`の有料化は将来判断できる独立した期間境界として保つが、現段階ではHome上でロックしない。

PICK UP項目カードは、通常の予定カード用248pt高を流用せず、内容204pt＋上下2ptの208pt高とする。左列は132pt幅の上174ptをアイキャッチ、下30ptをページ位置、右情報欄との間隔は12pt、カード内側余白は2ptで固定する。右欄は末尾4ptを操作枠線の安全域として使う。画像は対象アイキャッチだけを非同期縮小表示し、記録へ追加した写真は自動採用しない。対象アイキャッチがない場合も同寸法を維持し、ジャンル色＋アイコンではなくジャンル別の既定写真を表示する。気になるの右欄は`ジャンル / タイトル / 対象メモまたは未整理Inbox本文 / 公開期間 / 会場 / 公式サイト / 詳細を見る`、予定は`ジャンル / タイトル / サブ情報 / 日時 / 会場 / 公式サイト / 予定詳細・編集`、記録は`ジャンル / タイトル / 人物要約 / 日時 / 会場 / 公式サイト / 記録詳細・編集`を表示する。気になるの公開期間はカレンダー、会場はマップピンのアイコンを使い、登録がない対象と未整理Inboxでも行を消さず`ー`を表示する。公式サイト行はSafariアイコン＋`公式サイト`、未設定・無効URLはSafariアイコン＋`ー`とし、URL行だけを外部ブラウザへ接続する。申込・購入URLは表示しない。3タブすべてのタイトルは常に2行分の高さを予約し、3行目以降を末尾省略する。各状態で定義した最後の情報行の直下へ詳細・編集操作を置き、追加予定のない空スロットや操作列前後の可変余白は作らない。操作ボタンは高さ30pt、予定詳細／記録詳細と編集をともにNoto Sans JP 11pt Semibold、詳細側は残り幅へ収縮、編集幅64pt、ボタン間隔6ptとする。気になるの画像・情報欄・詳細ボタンはUUIDをHome親へ渡し、横ページングカード自身には遷移状態やSwiftData再取得Viewを持たせない。

`次の予定`の表示対象は、未終了の`Plan`に加えて、参加日を今日以降へ変更した`Visit`も含める。日付変更だけで暗黙の`Plan`は新規作成せず、未来日の記録を表示上の予定として扱う。同じ`Visit`に表示対象の`Plan`が紐付いている場合は`Plan`を優先し、二重表示しない。両者を日時順にまとめ、`Plan`からは予定詳細、未来日の`Visit`からは記録詳細へ遷移する。

セクションの`予定一覧`は独立した重複画面を作らず、下部のカレンダータブを`予定一覧`モードで直接開く。カレンダー画面は`カレンダー / 予定一覧`を切り替えられ、予定一覧では今後のPlanを日付順・月単位でグループ表示する。行から予定詳細を開き、要対応がある予定には期限アイコンを付ける。ジャンル絞り込みを許可し、完了済みVisitは通常の予定一覧へ残さない。中止・見送り・アーカイブはフィルターから確認できるようにする。

`Ticket Schedule`は画面幅を使う約106pt高・内側余白8ptの横長カードを縦に積む。左から`横書きの次対応名＋64pt正方形アイキャッチ / 縦2文字の「あ・と」＋大きな日数＋小さな「日」＋期限日＋期限時刻 / 区切り線 / 申込・販売区分＋購入先＋2行までの公演名＋参加日時 / シェブロン`の順とする。各列の基準間隔は4pt、期限欄は70pt幅とし、文字一体を狭めた欄の中央へ置く。区切り線は従来位置から6pt左へ移すが、線の右側へ6ptを補い、右情報欄の左端は動かさない。`あと`と`○日`は同じ42pt高の上下中央で揃え、`あと`14pt、日数38pt、`日`15pt、期限日・時刻14pt、曜日11ptを基準とし、日数だけ5pt上へ補正する。右情報欄は申込・販売区分列を2pt上へ移し、タイトルと参加日程の間隔を旧`-2pt`から3ptへ広げる。対応名は64×22ptの工程色ラベルとし、`抽選申込 / チケ発売 / 抽選当落 / チケ支払 / チケ取得`を字間を追加しない10pt文字で表示する。参加日入力だけは`参加日`を維持する。工程色はチケ発売をアンバー、抽選申込をワイン、抽選当落をパープル、チケ支払をティール、チケ取得をグリーンとする。区分・購入先は9pt文字とし、取得方法の固定色で塗った角丸4ptの四角形にクリーム文字を置く。抽選系はワイン、先着・一般購入系はティール、リセールはアンバーとする。区分と購入先は別ラベルで横並びにし、同じ文言なら1個だけ表示する。区分は全文表示を優先し、購入先は残り幅で可能な範囲まで表示する。通常の日数、期限日、曜日、期限時刻はBold、参加日時はSemiboldとする。期限日は`7/27(月)`の半角括弧、その下は`10:00`形式とし、主文字色78%で表示する。各TicketAttemptから現在状態に対応する次工程だけを1項目出し、抽選応募予定は申込開始/締切、申込済みは当落発表、当選/入金待ちは入金締切、受取待ちは任意のチケット受取開始へ切り替える。状態変更後に完了済み工程の日時を再表示しない。期限超過を先頭、その後を日時順に並べる。期限日と公演参加日は別の情報として同じカード内に表示する。取得済みで参加日未定の場合は左を`参加日`、期限欄を`未定 / 日程を入力`、情報欄を`参加日未定`とし、取得状態へ変更した操作UIを閉じた後に参加日入力シートを開く。日時確定済みなら入力を重複要求しない。期限情報自体が未登録の場合は`要確認・期限を入力`とし、参加日未定と誤表示しない。カードタップは下からクイック操作シートを開き、状態更新と参加日設定を行える。Home本文のジャンル移動スワイプと競合するため、カードの左右スワイプ操作は持たない。見出し右の`すべて見る`はチケット管理ページを開き、0件時はセクション自体を表示しない。発売・当落・入金・チケット受取の日時到達後または会員期限が近い場合だけ、通知ベルへ赤いマークを表示する。ベルを開いただけでは消さず、チケット状態の更新または期限対象外への変化で消す。ベル内のチケット期限行はPlan IDとTicketAttempt IDの両方を渡し、参加記録作成済みでも観劇記録詳細へ切り替えず既存の予定・チケット詳細を開く。該当するTicketAttemptカードまで自動移動し、ジャンル色の2pt枠と弱い影で強調する。Performance LogなどVisitを起点とする入口は観劇記録詳細を維持する。

Ticket Scheduleカードから開く個別シートは一覧ページの`チケット管理`と区別して`進捗管理`と呼ぶ。初期高は画面の66%を基準にし、Progress、次対応、状態更新、参加日設定、詳細導線を同じ画面内で読み取れるようにする。上部はHomeカードと同じく、申込枠とチケットサイトを別々の取得方法色チップ、公演名、参加日時の順に置く。申込枠チップはチケット、サイトチップはSafari、参加日時はカレンダーのアイコンを使い、未定時は警告付きカレンダーとオレンジで示す。Progressはジャンル色へ依存させずHomeの工程ラベルと同じ意味色を使い、申込=ワイン、発売=アンバー、当落=パープル、支払=ティール、取得=グリーンとする。表示用語は`支払 / 支払締切 / 支払待ちにする / 支払済み・取得済みにする`へ統一し、内部状態と保存フィールドは変更しない。完了した丸と線は各工程色、現在の丸は各工程色の二重枠、未到達はグレーで示す。日付は利用者が編集できる工程予定だけを出し、支払締切またはチケット受取開始が未入力・削除済みなら、状態更新日の代用表示をせず`—`とする。操作ボタンは14pt Semibold、ラベル基準高26pt、small controlとし、内容が収まらない場合はシート内を縦スクロールする。公演参加日時の右側に鉛筆アイコンを置き、申込／発売、当落、入金、受取の工程日を編集する。編集結果は同じTicketAttemptを通じてProgressとHomeカードへ同時反映する。

チケット管理一覧は、HomeのTicket Scheduleから情報の見え方が急に変わらないことを優先する。カード上段を`現在工程・64ptアイキャッチ / 期限・残日数 / 区切り線 / 申込枠・購入先・公演名・参加日時`の共通順とし、管理画面だけその下へ日付付きの全工程Progressと次対応を追加する。背景は入口ジャンルにかかわらず淡いニュートラル＋白カードのライト基調とし、工程色と取得方法色を情報の意味にだけ使う。状態フィルターは表示対象を絞るだけでなく、すべて=申込まとめ、要対応=入力不足／次対応、進行中=現在工程、取得済み=参加日確定状況、終了=終端状態、非表示=非表示項目へSection構成を切り替える。左右スワイプの存在は短い色線でなく、本体と同じ高さの緑・赤の背面カードを左右へ6ptずつずらした重なりで示し、スライド後はネイティブの操作メニューを表示する。

通常の残日数は「あ」「と」を回転させず縦に積み、その右へ大きな日数と小さな`日`を置く。日数文字とカード枠は同じ緊急色へ連動させ、通常はライトで濃紺、ダークでアイボリーのニュートラル色と細枠、明日は赤系1.5pt枠と弱い赤影、今日は強い赤系1.8pt枠と赤影にする。今日が期限でまだ期限時刻前のカードは、Home表示時およびアプリが再びアクティブになった時にカード最外周の赤いシャドウを約2回強弱させる。赤影が強まる各タイミングにカード全体を最大2.5ptだけ左右へ揺らし、小さく振幅を落としながら中央へ戻して、約2秒以内に静止表示へ戻す。工程・プレイガイド等の内側ラベルには影を波及させず、揺れでカード間隔・タップ領域・ジャンルスワイプ判定を変えない。期限超過・明日以降は動かさず、「視差効果を減らす」が有効なら今日でも明滅と揺れを両方行わない。期限時刻を過ぎたら同日でも`期限超過`へ切り替え、濃赤文字・赤枠・薄い赤のカード全面で示す。

入場準備確認シートは開催2日前から表示し、開催日との日差に応じて`明後日は「公演名」です。 / 明日は「公演名」です。 / 今日は「公演名」です。`を明示する。前日まではチケット・持ち物・移動予定、当日はチケット・持ち物と余裕を持った出発を案内する。公演の前日20時通知と当日9時通知も、それぞれ`明日は`と`今日は`を本文先頭へ置いて準備行動を添える。

チケット追加機能をHomeの独立メニューとして常設せず、期限や当落など利用者の対応が必要な時だけ`Ticket Schedule`へ表示する。

ジャンル入口は大きなタイルやアイコンを使わず、`総合 / 観劇 / 美術展 / ライブ...`の短い名称を明朝体の1段横スクロールで表示する。先頭の`総合`は全件一覧ではなく、予定・チケット・最近の記録をジャンル横断でまとめるHomeを意味する。Homeとジャンルトップで同じ`GenreNavigationStrip`を使い、ジャンル間は細い縦線、選択中はジャンル色の文字＋2pt下線、メニュー下は全幅1pt線とする。ジャンルトップ中央のジャンル名は見出しに限定し、直下メニューと重複する▼、タップ選択、一覧ダイアログは置かない。総合Homeは`Ticket Schedule`を含む本文全体へ1個だけ方向限定スワイプ認識器を適用し、内部で横ページングを使う`PICK UP`だけを明示的な除外領域にする。方向限定パン成立時はUIKit側で内側カードのタップをキャンセルし、タッチ中の本文へ`.disabled`を掛けない。確定先ジャンルのUUIDはHome親画面の単一`navigationDestination(item:)`へ渡す。左スワイプでは画面上の右隣へ進む`総合 → 先頭ジャンル → … → 最終ジャンル → 総合`、右スワイプではその逆順に循環する。左右24ptの画面端、`PICK UP`、ジャンルトップHero、内側の横スクロールチップは認識対象外とする。実機でTicket Schedule上の左右スワイプ、PICK UPの横ページング、縦スクロールとの干渉、縦線と下線のコントラスト、選択ジャンルの自動中央寄せを確認する。

Homeの英字セクション見出し（`PICK UP / Ticket Schedule / FAVORECO REPORT`）はCormorant Garamond 22pt Semiboldとし、左上 `Favoreco` と同じ適応ブランド色で表示する。旧`Interesting / Coming Up / 最近の思い出 / 最近の記録 / 横断ミニ統計`は独立セクションとしてHomeへ重複配置せず、PICK UPとFAVORECO REPORTへ統合する。

Homeの`Ticket Schedule / PICK UP / FAVORECO REPORT`と各ジャンルトップの主要カード外枠は、観劇カードを基準に連続角R=8へ統一する。PICK UP／Reportの選択タブ上角は外枠内へ沿うR=7とする。画像、地図、チップ、ボタン、詳細オーバーレイは用途別の角丸を維持する。

`PICK UP`の情報欄はジャンルの違いにかかわらず、対象メモ／受信内容、サブ情報／人物要約、日時、会場、公式サイトをNoto Sans JP 12pt Regularへ統一する。前4種は下部サンプル説明と同じセカンダリ色、公式サイトだけはリンク色を使う。サブ情報または会場が未入力でも行を詰めず、`ー`を表示して未入力と分かるようにする。

PICK UPの`予定`は月日・曜日を維持し、観劇／LIVEだけ開場登録済みなら`開場 HH:mm`、未登録なら開始時刻を`開演 HH:mm`として扱う。未来日の観劇／LIVE記録は開始時刻を`開演 HH:mm`、他ジャンルは開演表記を使わない。

Homeは全ジャンルの予定、期限、思い出をまとめる横断ダッシュボードとし、公式サイトに合わせたクリーム色を基調に、桃色・青緑・ベージュがごく薄く移る背景と微細な紙粒子を使う。青緑のニュアンスは残すが、桃色・薄赤ベージュがわずかに優勢な配色とする。紙粒子は操作を妨げず、カードや文字の可読性を落とさない固定背景とする。ジャンル固有の対象一覧や長い記録一覧は置かない。

Homeへ長い記録一覧、対象一覧、詳細統計、全件表示ボタンは置かない。Homeは最新10件までのPICK UPと現在期間のFavoreco Reportに限定し、全件の記録は記録タブ、ジャンル別対象はジャンルトップ、詳細統計とレポート作成は統計タブで扱う。

Homeに記録追加ショートカット帯は置かず、追加導線はまず下部の`追加`タブへ集約する。実機操作で主要導線が遠いと判断した場合のみ、役割が重複しないショートカットを後から追加する。

## 4.1 更新案内

アプリのMarketing Versionが前回確認済みのバージョンと異なる場合、オンボーディング完了後の最初の起動で更新概要をシート表示する。同じバージョンでは繰り返し表示しない。新規インストールではオンボーディング前に現在バージョンを確認済みとして保存し、初回利用を更新案内で遮らない。

更新概要はネット接続なしでも読める短いアプリ内データとし、版番号、見出し、要約、主な変更を表示する。`詳細な更新履歴を見る`は`https://ranoviqo.com/favoreco/`を開く。Web側の掲載先をNotion等へ変える場合はこの固定URLから転送し、アプリ更新を不要にする。設定の`サポート > 更新履歴`では、確認済みを含む全更新履歴をいつでも閲覧できる。

## 5. ジャンルトップ

`ランダムグッズ`は通常のVisit一覧ではなく収集シリーズ一覧を主役にする。カードにはシリーズ画像、シリーズ名、`収集済み種類 / 全種類`の進捗、ダブり数を表示する。シリーズ詳細は収集中／コンプリート、所持総数、ダブり、未入手、関連支出を上部にまとめ、その下に`すべて / 未入手 / ダブり`フィルターと種類別グリッドを置く。種類カードの`×N`は現在所持数であり、同じものを複数入手しても種類数は1件として数える。追加タブで新規シリーズを選んだ場合はシリーズ登録、既存シリーズを選んだ場合は入手・手放し履歴へ直接進む。

主役はジャンル別の対象ライブラリとする。

表示順:

1. ジャンル切替ヘッダー
2. ジャンル内ミニ統計
3. 直近の予定/要対応（存在時のみ）
4. 対象ライブラリ
5. 最近の記録
6. 検索、並び替え、絞り込み

対象単位の例:

| ジャンル | 対象 |
|---|---|
| 映画 | 作品 |
| 観劇 | 公演 |
| ライブ | ライブ・イベント |
| 本 | 書籍 |
| ミュージアム | 展示・イベント |
| お酒 | 銘柄 |
| 御朱印 | 寺社 |
| おでかけ施設 | 施設 |

ジャンルトップはHomeの縮小版にしない。横断情報や他ジャンルのギャラリーは置かない。

おでかけ施設はパークブルー`#2F7FB8`を標準ジャンル色とする。背景は白を主体に上部へ薄い水色のグラデーションを重ね、選択状態、カード枠、ボタン、ミニ統計、訪問場所MAPのマーカーへ同じ青を展開する。濃紺は文字色と写真のコントラストを支える補助色とし、画面全体を暗色にはしない。

従来の`美術展`は利用者向け名称を`ミュージアム`へ変更し、美術館・博物館・科学館・資料館の展示／イベントを扱う。上部統計は`訪れた館 / 鑑賞イベント / 気になる`とし、前者はPlaceMasterまたは入力済み館名の重複なし件数、後者は鑑賞記録がある展示・イベントの重複なし件数とする。館名未入力の記録は訪れた館へ推測加算しない。

`Coming Up / Interests`Heroはタイトルが1行でも2行分の高さを確保し、その下にサブタイトル、日付、会場を積む。サブタイトルは通常のセカンダリ色より少し濃くする。予定カード下部は`予定詳細 / 編集`とし、主ボタンを12pt、編集を10.5pt、アイコンと文言の間を3pt、高さを34ptへ揃える。画像・情報欄・`予定詳細`は同じ予定詳細へ進み、編集だけは保存済みPlanの編集シートを直接開く。対象一覧タイルは作品・公演単位の対象詳細であり、予定Heroの遷移先とは役割を分ける。

ジャンルトップもHomeと同じ固定ヘッダー部品を使う。左はHomeへ戻る27ptの控えめな`Favoreco`、中央は現在のジャンル名をNoto Serif JP 20pt Boldで表示する。ジャンル名が3文字以下なら文字間隔5pt、4文字以上なら文字間隔0とする。右はHomeと同じチケット管理、通知ベル、プロフィールを置く。システムNavigationBarの戻る・追加ボタンは重ねず、ヘッダー下には現在ジャンルのメインカラーを反映した全画面幅1ptの区切り線を置く。

観劇ジャンルトップは舞台の章として専用外観を使う。背景は黒へ沈むワインレッド、カード本体は8pt角丸と細いゴールド枠、文字はアイボリーとゴールドを基準にする。左上の`Favoreco`は金〜銅のグラデーションにする。ポスター/アイキャッチだけは角丸を付けず、一般的な縦長ポスターに相当するB判比率`1:1.414`の固定枠へ中央基準のaspect fillで表示する。カード本体やボタンの角丸は残す。観劇は共通の混在Heroを使わず、`Coming Up → チケット管理 → Interests → Performance Log → Productions → データ`の順とする。Coming Upは日時確定済みの予定を、縦長ポスター＋公演名・日付・開演時刻・会場、`Ticket Progress`＋先行区分・プレイガイド・工程タイムライン、`チケット / 詳細`の高さ28ptの小型カプセル2個の3段で表示する。カード内余白は10pt、主要ブロック間は8ptを基準とし、Plan編集はタイル右上の28pt鉛筆アイコンへ移す。予定チップの横は、非表示でないAttemptに`issued / attended`があれば`取得済み`、なければ申込なし・落選のみ・取得前を含め`チケット未取得`を表示する。ポスター部と`詳細`は同じ予定詳細へ進み、カード内に遷移先を持たずPlan UUIDを親画面の単一遷移先へ渡す。複数申込は先頭のProgressと残件数を示す。チケット管理は参加日未定の進行中申込、Interestsは予定・申込のない気になる公演、Performance Logは観劇記録、Productionsは状態にかかわらない登録公演のマスター一覧である。独立`Coming Up`の背面日本語は観劇=`観劇予定`、LIVE=`ライブ予定`、ミュージアム=`展示予定`、映画=`映画予定`とし、`これからの〜`は使わない。Performance Logは日付の隣へ開演時刻を並べ、カード右上へ17:00未満を`マチネ`、17:00以降を`ソワレ`とするゴールドのカプセルを表示する。マチネ／ソワレは観劇だけの表示とし、LIVEや他ジャンルへ流用しない。Productionsの3表示形式は、複数公演地を持ち得る公演マスターへ1つのPlan/Visitの日付・会場・評価を代表表示せず、公演種別・シリーズ名・主催など公演自身の情報だけを表示する。ライブジャンルトップは見出しとジャンル列を`LIVE`表記にし、紺から黒紺へ沈む背景、ティールのアクセント、青白い本文色を使う。観劇と同じ構造のミニ統計、`Ticket Progress`、`Coming Up`を使い、参加・記録済みの公演一覧は`Live History`とする。LIVEの`Interests` / `Live History`は横長バナーだけを使い、表示切替は置かない。

2026-07-25以降、Coming Upカード下段の旧`チケット`表記は`チケット追加`へ変更する。この入口は新しい申込を追加し、既存申込は`Ticket Progress`右の`日付編集`から編集する。チケット追加／編集シートは左上を34ptの×、中央を省略しない1行タイトル、右上をジャンル色の塗り保存カプセルとし、観劇では保存をゴールドで強調する。

Performance Log見出し右へ、公演情報と同じ外観の`2列 / 横長`切替を置き、選択を端末へ保存する。横長のマチネ／ソワレはタイトル行の右端ではなく、日付・開始終了時刻行の右側へ置く。タイトルは最大2行の全幅を確保し、日付・時刻は狭幅時だけ縮小して1行を維持する。ラベルは`公演情報`タグと同じ18pt高・左右6ptを基準とし、外枠を付けない角丸4ptの小型表示にする。マチネは太陽アイコン＋暖色ゴールド、ソワレは月アイコン＋青みのある月光色で区別する。2列カードはProductionsに倣った112pt高とし、上下8ptを除く画像高96ptからB判比率`1:1.414`で幅約68ptを算出する。10.5pt Boldのタイトル・8.5pt補足を基準とし、タイトルは2行分を常時確保、その下を`参加日 / 開始終了時刻 / 会場`の3行に分ける。日付はカレンダー、時間は時計、会場は場所アイコンを使い、長い時間範囲はその行だけ縮小する。マチネ／ソワレは2列の表示項目へ追加しない。両表示は同じ記録詳細を開き、最大10件と`さらにn件`の展開規則を共有する。時刻による17:00境界と観劇限定の表示条件は維持する。

観劇Coming Upから開く予定詳細と観劇済み詳細は、別の予定版を作らず共通`TheaterExperiencePage`を使う。同じ舞台Hero、本文余白、ワイン背景、暗色化を維持し、状態に応じて存在するデータだけを追加する。公演名、シリーズ、主催、公式情報、あらすじ、ポスターはリンク済み`ExperienceEvent`から、日時、会場、予定メモは`Plan`から、座席、先行区分、プレイガイド、Ticket Progressは`TicketAttempt`から直接表示する。同じ値をPlanへ複写せず、公演情報の変更はすべての予定・記録へ自動反映する。参加公演詳細のアイキャッチは、公演情報詳細のポスターと同じB判比率・角なし・金二重枠・黒シャドウを使う。公演情報詳細ではポスター上端を左右50pt丸ボタンの下端付近まで下げ、シリーズ名・タイトル・公演情報・操作もポスターに続けて下げる。予定段階では評価、感想、当日写真などVisit固有情報を置かず、参加記録保存後に同じ詳細へ追加する。`Plan.visit`が存在する場合はPlan経由でも観劇済み内容を直接開く。予定中の表示順は`Hero / 作品・公演情報 / 次にやること / チケット / 準備 / 費用 / 予定メモ`とし、予定編集はPlan、公演情報詳細・編集はExperienceEventへ分ける。

公演名の正本は`ExperienceEvent.title`とする。観劇Coming Up、予定詳細、予定編集はリンク済み公演名を優先し、予定編集では公演名を読み取り表示にして個別変更させない。既存バックアップ互換の`Plan.title`は公演情報編集時と予定保存時に正本へ同期するが、予定固有の`Plan.subtitle`は保持する。記録詳細と予定詳細のタイトル／`公演情報を開く`は、SwiftData実体を直接遷移先へ渡さず公演UUIDだけを渡し、遷移先で再取得する。

観劇ジャンルトップからこの詳細を開く時は画面遷移せず、ジャンルページを背面に残して黒62%で暗転し、その上へ左右10pt、上部Safe Area＋42pt以上、下部Safe Area＋8ptの18pt角丸パネルとして四辺を離して重ねる。予定と観劇済みは同じオーバーレイ選択状態を使う。パネル表示中だけ標準Tab Barを隠し、閉じるとTab Barとジャンルトップの元のスクロール位置を復帰する。Safe Areaは外側パネルが一度だけ処理し、内側詳細で二重に加えない。Heroはパネル上端から始め、閉じる／三点操作はHero写真の上へ重ねる。細い金枠はスクロール本文ではなく固定パネル外周へ置き、上部操作を含むパネル全体を囲む。本文は`LazyVStack`で構成し、Map、写真、動画等の重い内容は画面内へ近づいた時に生成する。詳細タイル表示中は背面ジャンルトップのScrollViewだけを停止し、全画面暗転オーバーレイで背面タップを遮断する。ジャンルトップ根本のヒットテストは切らず、閉じた直後にジャンル切替を再開できる状態を維持する。縦ドラッグは詳細タイル本文だけ、横方向優位の右スワイプは詳細タイル全体の終了操作だけが受け取る。

現在表示中の申込を対象にした`日付編集`を置く。Coming Upカード内と公演詳細のチケット管理ではTicket Progress見出し右、観劇ジャンルトップの`Ticket Management`では選択中Progressカードの金枠内下段右に置く。観劇ジャンルトップの見出し右端は`すべて見る`だけとする。申込／発売、当落、入金期限、チケット受取の日付は同じ`TicketAttempt`編集画面で変更し、保存後はProgressとHome Ticket Scheduleを同時更新する。工程の現在位置は日付の新旧ではなく申込状態から決めるため、過去工程の日付だけを修正しても現在工程を巻き戻さない。

ジャンル切替はHomeと共通の1段横スクロールを画面冒頭へ置き、現在ジャンルを色つきアイコンと下線で明示する。従来のHero内プルダウンは使わない。ジャンルトップの共通ヘッダーは左ブランド、中央ジャンル名、右のチケット・ベル・プロフィールを同じ横並びでレイアウトする。右の3操作は固定幅を確保し、中央ジャンル名は残り幅だけを使って縮小するため、`テーマパーク`等の長い名称も操作へ重ねない。観劇以外の御朱印以外Heroは`Coming Up / Interests`として、未来Planを開始日時の近い順、その後へ興味あり対象を更新日の新しい順に並べ、合計最大10件を横スワイプ表示する。同じ対象に未来Planがある場合はPlan側だけを表示する。ミニ統計は全ジャンルで観劇と同じ構成を使い、1枚の3列横長パネル、アイコンなし、隣接項目の細い縦線、各列を上段`12pt名称`、中央`33pt数値＋単位`、下段`9pt補足`で組む。名称と数値の間は1pt、数値と補足の間は2pt、上下余白は10ptを基準とする。文言はジャンル別とし、観劇は`作品・公演 / 観劇済み / 気になる`、ライブは`ライブ / 参加済み / 気になる`、映画は`映画 / 鑑賞済み / 観たい`、本は`本 / 読了 / 読みたい`などとする。観劇は上側をわずかに明るく下側を黒に近づけたワイン系グラデーション、淡い金光、0.7pt金枠、アイボリー文字を維持する。ライブは紺〜黒紺の弱い縦グラデーション、ティールの光・枠・区切り、青白い文字を使う。その他は白系の弱い縦グラデーション、ジャンル色の光・枠・区切り・単位色を使う。内部実装用の有効ユニット数は見せない。観劇の申込管理は`Ticket Management / チケット管理`、公演マスターは`Productions / 公演情報`、ライブは`Coming Up / Interests / Live History`を主要見出しとして表示する。`Ticket Progress`は小項目として英字だけを使う。その他カード外周は0.75ptを基準とする。背景は白ベースを保ちながら、現在ジャンル色を上部へ薄く反映する。

全ジャンルトップの大見出しはHomeの`PICK UP`と同じCormorant Garamond 22pt Semiboldの英語を前面に置き、長い英字は1行のまま最小78%まで縮小を許可する。Noto Serif JP 20pt Semiboldの日本語は約11%濃度で17pt右・10pt上へ置き、各文字間へ半角スペースを1個入れて表示する。Hero、ライブラリ、御朱印、シリーズ、訪問MAPなど章内を区切る主要見出しだけを対象とする。`Ticket Progress`はカード内または小項目の見出しなので対象外とし、日本語背面文字を付けない。カード内見出し、ボタン、空状態、ミニ統計、Home、FAVO、詳細画面も対象外とする。VoiceOverは装飾用の空白を読まず、元の日本語見出しを1回だけ読み上げる。

観劇を選択中の下部`追加`メニューは、`公演・チケットを登録 / 観劇の思い出を記録`の2項目を表示する。前者は`気になる / 予定 / 申込 / 取得済み`を1画面先頭で選び、必要な公演・予定・チケット情報だけを展開する。後者は参加日時確定済みPlanの未記録なら新規Visit、記録済みなら既存Visit編集へ進み、予定なしの過去記録も選べる。通常用の`予定を立てる / 体験済みを記録 / クイック登録 / 申込・発売を登録`は観劇中に表示しない。シートは`追加`押下時の観劇UUIDを持つ単一の表示リクエストから描画し、ジャンルUUIDと表示フラグを別Stateで同時更新しない。

予定・チケット登録は`公演の基本情報 / 観劇予定日 / 会場 / 公演URL / チケット情報`へ区切り、公演名、サブタイトル、会場、住所、公式URLを上段項目名／下段入力欄の二段構成にする。観劇以外は`観劇予定日`を`予定日時`とする。登録済み会場候補は、名称と、郵便番号・空白・ハイフン表記を吸収した住所で同一性を判定し、同じ会場を1件だけ表示する。住所がない候補は座標を使い、同名でも所在地が異なる施設は別候補として残す。この重複抑止は予定入力と記録入力で共用し、元の場所マスターは自動削除しない。

2026-07-28以降、観劇の公演登録と予定・チケット登録では`公演URL`を独立した区分にせず、`公式URL`行として`公演の基本情報`カード内の末尾へ統合する。公演登録の`URLから登録 / 画像から登録 / 気になる公演に登録 / 観劇予定日を入力 / チケットスケジュール設定`はタイトル寸法を維持し、説明文だけNoto Sans JP 11pt Regularとする。

総合と観劇の`Coming Up`行は共通部品を使い、左から`24ptの月/日と12ptの日本語曜日を縦2段`、`64pt正方形アイキャッチ`、`ジャンル・タイトル・会場を2pt間隔で縦3段`、右端シェブロンの順にする。画像なしはジャンル色のアイコンで代替する。タイトルは2行、会場は1行までとし、総合は白系カード、観劇はワイン系カードと金枠を使う。

ジャンルトップの左右スワイプはHeroを除き、統計から章末までの本文で前後ジャンルへ切り替える。Heroは自身のカルーセル操作を優先し、ジャンル切替を開始しない。公演選択チップ、チケット情報チップなど内側に横スクロールを持つ場所でも内側操作を優先する。映画を含む全ジャンルで同じ共通ジェスチャー部品を使い、認識器はジャンル内容の`.id`と遷移Viewより外側に1個だけ維持する。これにより切替アニメーション中も新旧ジャンルの認識器を重複させず、前後方向を同じ条件で判定する。判定用マーカーは本文と同じ実サイズへ広げる。左右24ptの画面端はシステムの戻る操作を守る。横スクロールチップは自身の表示枠でクリップし、カードや画面の外へ描画しない。

美術展・LIVE・おでかけ施設は対象一覧と段階展開ボタンの後、章末カードの前へ`行った美術館・会場MAP / 行ったライブ会場MAP / 行った施設MAP`を表示する。同じ場所の記録は1地点へまとめ、訪問回数が多いほどジャンル色のマーカーを濃く大きくする。座標はVisitを優先し、未設定ならPlaceMasterを使う。上部は`全国 / 北海道 / 東北 / 関東 / 中部 / 近畿 / 中国 / 四国 / 九州・沖縄`のチェック式単一選択を横スクロールし、選択地域へカメラを移動する。`詳細検索`は施設・会場・住所・記録内容を対象とする自由ワードと都道府県を持ち、適用すると該当地点へ再フォーカスする。位置未設定はMAP対象外として件数を下部へ示す。MAP内のパン・ズーム、エリア列の横操作はジャンル切替ジェスチャーを開始しない。

対象一覧の見出し右へ、3列ギャラリー／2列ミニ詳細／横長バナーの切替アイコンを置く。ただしLIVEは横長バナー固定としてアイコンを置かない。選択はジャンル単位で保存する。ギャラリーは画像と情報帯を同じ背景・枠線のタイルへ一体化し、`7/7 (火)`形式の日付を左、ジャンル色の短い縦線、星＋評点を右へ1行表示する。未記録は`—`、土曜は青、日曜と平日の未来予定日は赤字。ただし観劇の`Productions / 公演情報`は、同じ公演へ複数回の予定・記録を紐づける親一覧であり、ポスターから公演を識別する用途を優先するため、3列表示だけ下段情報帯を外して公演ビジュアルのみを並べる。公演名・公演種別・シリーズ・主催はVoiceOverで読み、観劇の2列・横長表示には従来どおり文字情報を残す。映画・ミュージアム等の3列情報帯は変更しない。2列ミニ詳細は列間を12ptとし、左右の枠線を離す。タイトルはゴシック体へ変更し、従来より2pt小さく、字間`-0.35`・行間`-1.5`で2行分を常時確保する。通常カードは106pt、映画カードは108pt固定高とし、題名が1行でも2行目を空白として高さを揃える。2列の日付は先頭へカレンダーアイコンを置き、`7/16 (木)`の半角括弧にし、従来より1pt小さくする。評価済みは0.5刻みに丸めた約7.8ptの5つ星と9.35ptの正確な数値を1行に並べ、未評価は`☆ —`とする。映画ではB判ポスターをカードの縦いっぱいへ表示する。バナー画像は保存済み比率枠へ中央aspect fillし、状態・2行タイトル・日付・会場を右へ表示する。映画・書籍・おでかけ施設の共通ファーストビュー型Heroは、アイキャッチを内側幅45%・最大152.5pt（従来比1.25倍）とする。作品タイトルは18.5ptを基準に固定の空行を予約せず2行まで、右側情報の行間は4ptを基準とし、周辺情報を圧迫しない。

表示切替のStateと保存値はテンプレートキー単位に分離し、美術展を含む御朱印/LIVE以外のジャンルで同じ操作にする。LIVEは保存済みの旧選択値があっても横長バナーを優先する。2列ミニ詳細は日付と評価の間に補足行を置き、観劇は会場、映画は映画館名、おでかけ施設は予定／記録の場所住所から抽出した都道府県を表示する。未設定は`—`で行を保ち、従来の固定高は変えない。

御朱印の専用Heroは最近いただいた御朱印を新しい順に最大5件表示する。現在の和紙カードサイズを変えず、前後カードを見切れさせない全幅ページ式とし、2件以上の時だけ下へ丸インジケータを表示する。統計は`参拝先 / ご記帳済み / 気になる`の3列とする。参拝先は御朱印Visitが紐づくEvent IDの重複除外数で、同じ寺社への複数参拝と未参拝の「気になる」寺社を含めない`総参拝先数`、ご記帳済みはVisit単位で御朱印をいただくたび増える`総御朱印数`とし、単位はそれぞれ`寺社 / 印 / 寺社`とする。

一覧の初期表示は3列ギャラリー9件、2列ミニ詳細8件、横長バナー6件とする。残件があれば区切り線付き・14pt基準・44pt押下高の`さらにn件`を表示し、タップごとに同数を追加する。全件表示後は`閉じる`へ切り替え、初期件数へ戻す。各一覧自身が表示件数を保持し、展開状態はジャンル・セクション・表示方式ごとに分ける。観劇・美術展・LIVE・映画では`stateKey == interested`を`Interests`として通常一覧より前へ独立表示し、その直後へ`Coming Up`を置く。未来Planが紐づく興味あり対象は`Coming Up`だけへ出し、通常一覧を含めて重複させない。`Interests`は0件でも見出しと空状態を表示する。本は通常一覧前へ`気になる！ → 積読`を置き、興味ありを前者、興味ありではなくVisitがない本を後者、Visitがある本を通常一覧へ分類する。両方とも0件時に見出しと空状態を表示する。横長バナーは正方形トリミングをやめ、82pt幅で登録済みアイキャッチ比率を維持する。情報欄は9pt・18pt高の状態チップ、2行分を常時確保した15ptタイトル、日付、会場の縦順とする。状態は有効なチケット工程を最優先し、`チケット発売待ち / 当落待ち / 入金待ち / 発券待ち / 発券済み`などを表示する。工程がなければジャンルに応じた予定・体験済み・気になるを表示する。LIVEは工程がない場合を`参加予定 / 参加済み`とし、シリーズ/ツアー名、出演者、主催も入力済みの範囲で表示してテキスト情報を優先する。観劇ジャンルトップは`ExperienceEvent.isArchived == true`の公演が1件以上ある時だけ本文下部へ`非表示の公演 n件`を置く。押すと公演ビジュアル、タイトル、シリーズ、観劇記録件数の一覧を開き、各行の`再表示`またはスワイプから復元する。復元では`isArchived`と更新日時だけを変更し、観劇記録・予定・チケット・写真を保持する。0件時は入口を表示しない。

各ジャンルトップの`Interests`は、0件の空状態カード内だけに現在ジャンルの追加操作を置く。文言は観劇=`公演を追加`、ミュージアム=`展示を追加`、LIVE=`ライブを追加`、映画=`映画を追加`、本=`本を追加`とする。1件以上ある時は表示しない。本の`積読`を含むInterests以外の空状態、Homeの`PICK UP`にはこの追加操作を置かない。

書籍は登録・編集の写真欄と対象アイキャッチ編集で本の種類を選択する。コミック`112:174`、新書`103:182`、ハードカバー`127:188`、文庫`105:148`、雑誌`182:257`、写真集・画集`210:297`の実寸比率をそのままアイキャッチ枠へ反映し、書影は中央aspect fillする。種類は作品と記録へ保存し、Home、ジャンルトップ、一覧、対象/記録詳細、入力サムネイルで共通利用する。新規既定はハードカバー、既存未選択データは従来の`1:1.45`を維持する。

観劇の`チケット管理`は対象0件でも管理画面への入口を残す。参加日未定の進行中申込を独立表示し、日時確定済みはComing Upカード内の進捗として表示する。工程は抽選の`申込・当落・支払・取得`と先着の`発売・取得`を基準に、不要な当落・支払を省く。先着の`発売`は発売開始日時を示し、`購入`は発売後の状態更新操作として分離する。発券は固定工程にせず、必要な場合だけHomeの`チケット受取開始`として扱う。観劇は取得工程から外す。工程名は丸内、日付は丸下、接続線は丸の外周間だけに置き、完了をジャンル色の実線、未来をグレーの点線、現在をジャンル色の二重丸で表す。見出し右端の`すべて見る`とComing Up内の`ほかn件のTicket Progressを見る`は、親NavigationStackへ直接pushせず、独立した`NavigationStack`付きシートで`チケット管理`ページを開く。選択中の申込に対する`日付編集`はProgressカードの金枠内下段右へ置く。管理ページ上部は検索、件数付き状態チップ、現在チケットがあるジャンルのチップを2段で置く。各申込は独立した16pt角丸カードとし、上段に区分・先行/プレイガイド・状態、中央に44×58ptアイキャッチ・公演名・参加日時・小型Progress、下段に区切り線付きの`次にやること / 具体操作 / 期限日時`を置く。終端状態だけは`現在の状態`とする。カード左右端へ7×42ptの緑／赤のベロを4ptだけ見せ、右スワイプの次状態、左スワイプの落選/見送り・編集を常時示す。VoiceOverも同方向を案内する。カードタップは下からクイック操作シートを開く。取得済み・参加日未定は要対応として残し、参加日設定後にComing Upへ移す。ジャンルトップ等の入口から初期ジャンル・状態・対象公演を引き継ぐ接続は次段階とする。

御朱印以外のジャンルは共通ファーストビューを使う。これから参加予定のPlanと興味あり対象を横スナップカルーセルで並べ、予定を先、興味ありを後として合計最大10件にする。カードと複数件カルーセルは総合Homeと同じ248pt高とし、観劇・映画・美術展のB判アイキャッチ、他ジャンルの情報・操作を共通枠内へ収める。その下の集計も3列連結ミニ統計へ統一し、アイコン型4列パネルは使わない。続く対象ライブラリでは、観劇・美術展・LIVE・映画に`Interests → Coming Up`の独立一覧を残す。御朱印Heroは専用寸法を維持する。

横長バナーの日付行は`日付 時刻`とする。上段右は観劇で`主催: 名称`、映画で`監督: 氏名`を入力済みの場合だけ表示する。観劇の主催は予定の主催名、作品の主催名、人物・団体の主催役割の順で補完する。映画の監督は人物・団体の監督役割から表示する。未入力なら上段右の領域は表示しない。

## 6. 対象詳細

`ExperienceEvent`の閲覧専用画面。

置く情報:

- 対象名、ジャンル、シリーズ、サブ種別
- 代表写真
- 基本となる人物・団体
- 公式URL、対象メモ
- 関連する予定
- 体験履歴
- 記録回数、最新日、平均評価

置かない情報:

- 個別の体験日、座席、当日の金額、当日の写真
- 当日の天気、感想、同行者

これらは記録詳細へ置く。対象編集は別画面へ遷移する。

観劇だけは作品・公演詳細を複数回の観劇を束ねる公開公演ハブとして扱う。表示順は`ポスター中心Hero / 公演スケジュール / あらすじ / キャスト・スタッフ / Ticket Progress / 次の予定 / 参加した公演 / 金額総計 / 思い出ギャラリー / 遠征Map`とする。Heroは中央のB判ポスターにゴールド二重枠を付け、その下へシリーズ、タイトル、サブタイトル、全体会期、公演地要約、主催、公式URL・チケットサイト・SNS、予定追加と観劇記録追加をまとめる。対象編集では東京公演・大阪公演等の公演地名、個別の開始／終了日、会場、住所を1セットとして複数登録する。Heroは各セットから最初の開始日と最後の終了日を全体会期として算出し、1公演地では会場名、複数公演地では`東京・大阪｜2都市・2会場`のような要約だけを表示する。Hero直下の公演スケジュールは開催中・次回を優先して最大2件を表示し、全件数と`ほかn公演地を見る / 閉じる`で肥大化を防ぐ。住所は各公演地の行を開いた時だけ表示する。すべて終了済みの場合は日付順の先頭2件を表示する。旧形式の共通会期・会場は同じ`unitFieldsRaw`から後方互換読込し、未入力時は紐づくPlanとVisitの日時・会場から補完する。公開公演ページに座席・天気・単回記録用の地図は置かず、それらは参加した公演のVisit詳細で表示する。キャスト・スタッフは改行テキストをそのまま読みやすく表示し、旧形式の構造化リンクが残る場合だけ互換表示する。あらすじとキャスト・スタッフは初期状態を開き、見出しから個別に閉じられる。次の予定は未来Planを日時昇順で並べ、行から該当Plan詳細へ進む。Ticket Progressは対象に紐づく非表示でない全Plan・全TicketAttemptを対象とし、進行中だけでなく落選・参加済み・見送りも残す。参加履歴は新しい順に並べ、写真、日付、開演・終演時刻、場所、その回のお目当て・注目した人だけを表示し、各行から該当Visitの記録詳細へ進む。金額総計はチケット・グッズ・遠征・その他旧入力の4区分とし、同じVisitの二重集計を防ぐ。思い出ギャラリーは各Visitの`思い出`分類写真だけを、画像比を保った同一高さの横スクロールで表示する。その下の遠征Mapは参加した公演の会場をピン表示し、同じ場所マスターの参加回数をピンの数字としてカウントアップする。旧記録は正規化した会場名と近接座標でまとめ、位置未設定はMap対象外として件数を案内する。

公開公演の`次の予定`とチケット管理カードはPlan UUIDだけを親へ渡し、親の単一`navigationDestination`でSwiftDataから再取得して予定詳細を開く。各行がPlan実体と遷移先を個別に保持しない。Ticket Progressは`waitingIssue`では取得ノードを現在位置の二重枠、`issued / attended`では進捗位置を全工程の先へ進め、取得ノードまで完了色で塗る。

全ジャンルトップのHero／Coming Up／参加記録一覧等から開く予定・参加記録詳細は、暗転したジャンルトップの上へ共通パネルとして重ねる。Heroから本文末尾までを1枚の18pt角丸カードへ収め、観劇はゴールド枠、その他は現在ジャンル色の枠を使う。カード全体を単一の外側ScrollViewで上下に動かし、枠内に固定高の独立スクロール領域は作らない。上へ動かすと枠上辺とHeroが一緒に画面外へ移り、本文末尾では枠下辺が現れる。カード外側の下余白は置かず、固定`編集 / SHARE`バーの直前までカードの表示範囲を連続させる。`⌄ 閉じる`と三点操作はカード外の上部固定操作、`編集 / SHARE`は左右各1/2幅・高さ58ptで画面端まで伸ばすSafe Area内の下部固定操作とする。編集は予定／記録それぞれの既存編集画面へ接続し、共有文にはタイトル・日付・時間・会場・登録済み公式URLを含める。パネル内のタイトルまたは`公演情報を開く`から対象詳細へ進む時は、全面入力を遮断するパネルをアニメーションなしで先に閉じ、対象UUIDを親のジャンルトップへ渡し、次の描画タイミングで親の単一`navigationDestination`から再取得して開く。Heroを含む外側は通常`VStack`、画面外のMap・写真等を含む本文は`LazyVStack`で遅延生成する。遷移は全体の拡大縮小を使わず、18ptの短い下位置から0.18秒でフェードインする。右スワイプ終了は画面幅の左72%以内から開始でき、右へ80pt以上または予測移動420pt以上、かつ横移動が縦移動の1.35倍を超える時だけ判定して本文の縦スクロールと区別する。表示中は標準Tab Barを退避し、閉じると元のジャンル位置とTab Barを復帰する。

対象編集では対象名などの基本情報に加えて、対象アイキャッチを追加・変更・解除できる。解除するのは`ExperienceEvent.eyecatchData`だけとし、各記録に保存した写真は削除しない。対象詳細とジャンルトップで使う代表画像の優先順位は、利用者が明示指定した記録写真、対象アイキャッチ、記録写真からの自動候補の順とする。観劇では画面名を`公演情報を編集`、画像欄を`公演ビジュアル`とし、公演名、シリーズ、公演種別、公式情報、キャスト・スタッフの改行テキスト、公演団体をこの対象編集だけで更新する。公演登録の基本情報にも`公演ビジュアル`を常設し、取込アコーディオンを使わずローカル画像を選べる。通常選択は画像だけを更新しOCRを行わない。公演ビジュアルは150×212ptで中央配置し、右上のゴミ箱を押した後に削除確認を出す。写真変更はその下の小型中央ボタンとする。公演登録・公演情報編集・公演地編集・観劇予定・チケット手配の通常入力は、上段左の16pt項目名と下段いっぱいの16pt入力欄を持つ66pt基準行へ統一する。項目名と上枠・区切り線の間に9pt、行下に8ptを確保する。`任意 / 必須`は項目名の隣、プレースホルダーは入力欄内へ置く。プルダウン、日付、時刻、Toggle、数量は同じ二段構成とし、選択値を16ptで右寄せして右端から4pt追加で離す。行間の横罫線は`secondary 46%`で共用し、縦罫線は使わない。公演名はフォーカス時に行高が変わらない1行入力とし、長文は入力中に横スクロールして保存値を省略しない。SNSはX／Instagram／Threadsの3アイコンを横並びにし、タップで各URLの入力シートを開く。保存済みだけカラー、未登録はグレーで表示する。既存のSNS文字列から対象サービスを判別し、判別できないリンクも再保存時に保持する。公演登録のURL取得と画像・OCR取得は実線で区切る。ビジュアル解除は右上ゴミ箱＋確認とし、メモは5行分、座席は通常1行・長文時のみ最大2行を予約する。観劇記録では公演名・シリーズ、日付・時刻・鑑賞方法・会場・住所・評価、チケット状態・座席、金額、感想メモ、公式URL・SNS、OCR原文、人物名、自由感情タグを同じ二段構成にする。写真・人物候補・感情タグ候補のカード/グリッドは既存構造を維持する。保存カードはForm端から左右8pt離して角丸枠を保ち、88pt以上の高さと上下14ptの内側余白を持つ。必須入力完了時は淡いジャンル色の背景・枠・アイコンで活性状態を示す。

公開公演Heroの中央ポスターは対象アイキャッチを最優先とし、未登録時だけ記録写真由来の代表写真を使う。Hero背景は中央ポスターを拡大・ブラーせず、対象編集で選択した劇場背景JPEGをアプリバンドルURLから読み込み、Hero本体の実寸へ全面表示する。薄いワイン色と下方ほど暗いグラデーションを重ね、タイトル、会場、リンク、予定／記録追加の判読性を確保する。公式情報は`公式サイト / チケット / X / Instagram / Threads`を固定順で横並びにし、URLがない項目も位置を詰めずグレーの無効表示とする。公式サイトとチケットは文字付きボタン、SNSはアイコンで表示し、登録済みだけ有効色と外部遷移を持つ。中央ポスターはB判比率の148×209pt・ゴールド二重枠、Heroの最小高は620ptとし、予定／記録追加は横幅が足りない時に縦並びへ切り替える。画面左端からの右スワイプは公開公演ページを閉じ、キャスト横スクロール上の操作はキャスト閲覧を優先する。

公開公演の視覚トーンは、舞台背景、黒へ沈むワインブラック面、0.8ptの細いゴールド枠、アイボリーの明朝見出し＋ゴールドの用途別アイコンを基準とする。本文は`LazyVStack`で遅延生成し、カード間は8ptとして連続した舞台プログラムのように見せる。右上の三点操作はネイティブ`Menu`ではなく同じワインブラック＋金枠の画面内パネルとし、編集・代表写真・非表示・削除を直接開く。対象編集の公演ビジュアルはB判相当の150×212ptへ抑えて中央に置き、画像変更、背景3種、基本情報が同じ初期スクロール範囲で見えるようにする。

観劇の対象編集には`公演団体・関係者`を置き、人物／団体の区分と役割を分けて選ぶ。団体役割は`上演団体 / 主催 / 制作 / 企画 / 招聘・提供`とし、団体マスターで上位団体を設定できる。統計の`観劇・団体別`は公演数と観劇回数を団体UUIDで重複除外し、子団体の実績を親団体にも合算する。観劇回の感情タグは基本情報で複数選択し、記録詳細の感想内へ表示する。観劇回の`お目当て・注目した人`は、名前入力と既存人物サジェストを同じ入口にし、各人物へ`お目当て / 気になった / 刺さった / 尊い`を任意・複数選択できる。人物候補は汎用設定にかかわらず常時表示し、表示名・よみ・別名で検索する。候補選択または完全一致入力では既存人物マスターを再利用する。既存人物への変更も保存ボタンまでは編集Draftに留め、キャンセル時は保存しない。タグは人物カードに併記し、人物マスターやMY FAVOの属性としては表示しない。記録詳細の登録済み人物カードからは人物UUIDで人物マスター編集へ進み、名前・よみ・写真を後から補完できる。同画面のFAVO設定は明示的な任意操作とし、観劇回への登録だけでは自動作成しない。

## 7. 記録詳細

観劇予定／観劇済みの共通Heroは、カテゴリ用JPEG背景を表示し、下部をワイン色へフェードさせる。Hero直下には12ptの短いワイン色フェードを置き、その後の本文上余白を2ptとして、単色の境目や暗い帯を作らず`作品・公演情報`へつなぐ。左に幅140ptのアイキャッチ、右に15pt Regular・行間6ptの「日付・時間・形式・会場・席・評価」を6行固定で並べ、未入力は行を省略せず `—` とする。天気はHero上段の公演年／観劇回数または予定種別の右端へ16pt Regularで置き、日付行とHeroの縦幅を圧迫しない。未取得時は `cloud.sun` と `—`、取得済みの観劇記録では保存済みの天気アイコンと最高／最低気温を表示する。

`Visit`の閲覧専用画面。保存情報を原則すべて表示する。

観劇の表示順:

1. Hero: シリーズ名・観劇回数、タイトル、任意サブタイトル、アイキャッチ、日付＋天気、上演時間、観劇スタイル、会場、座席、評価
2. 作品・公演情報: 主催等、公式URL、チケットサイト、SNS
3. 次にやること: チケット申込・入金／発券・遠征ToDo
4. 会場、住所、埋め込みMap
5. 写真・コレクション: 思い出、グッズ、ノベルティ・特典
6. 感想
7. キャスト・スタッフ
8. チケット・座席: 半券／チケット写真、座席情報、申込情報
9. 費用合計
10. 当日の人物・団体、スタッフ・関係者
11. OCR・取込結果（初期状態は閉じる）

上部は全ジャンル共通の画像主役型Heroとする。標準ナビゲーションバーは完全に隠し、写真上へ独自の戻るボタンと右上メニューを重ねる。右上メニューはジャンル正規色を面、観劇ではゴールドをアイコンと枠の差し色に使い、独立したヘッダー帯を作らない。写真は上部の高さを制限した帯へ中央基準のaspect fillで配置する。縦長写真は天地を均等に切り、画像比率でHero高を押し広げない。白へのフェードは使わず、元画像の色を残したまま下端だけを該当ジャンルの正規色へ溶かす。ページ全体はジャンル色から深色へ続く暗色基調とし、観劇はワインレッド`#8B2F45`を面、ゴールドを操作と装飾の差し色に使う。明るい写真でもステータス、上部操作、Hero文字が沈まないよう、局所コントラストスクリーンと文字影を使う。対象名は帯の下端と重なる位置に置き、その下へ左アイキャッチ、右へ日付、上演時間、観劇スタイル、会場、座席、評価を置く。日付と時間は別行とし、時計行はアイコンと時刻だけを表示して固定の`上演時間`接頭語は付けない。終演が入力済みなら`12:00–15:00`形式で表示する。対象名は対象詳細へのリンクにする。背景またはアイキャッチとして使った写真は直下の写真一覧で重複させない。写真がない場合はジャンル色グラデーションとジャンルアイコンをフォールバックに使う。

観劇スタイルはタグアイコンと選択値だけをHeroへ表示し、固定の`スタイル`接頭語は付けない。`Visit.unitFieldsRaw`内の`styleNames`へ複数値で保存し、スタイルだけを入力した場合もJSONを保持する。旧記録は空配列として互換読込し、SwiftDataスキーマは増やさない。追加／編集フォームではイマーシブ演劇・大衆演劇を含む主要候補を複数選択でき、候補にない表記もカンマ区切りで入力できる。対象そのものの分類に使う`ExperienceEvent.subTypeKey`や、全ジャンル共通のタグとは分け、今回の観劇回の表示情報として扱う。

Heroは早見として日付、上演時間、観劇スタイル、会場、座席、評価までに限定する。席種は別項目を増やさず、座席へ`S席・1階A列12番`のように一体入力する。座席メモ、購入元、申込経路、券面、手数料は下部の`チケット・座席`へ集約する。TicketAttemptがある場合は当選以降の確保済み申込だけを表示し、落選・見送りは観劇回の詳細と費用へ混ぜない。Visit側のチケット状態は確保済みTicketAttemptがない場合だけ同カードへ表示し、基本情報と重複させない。Heroの座席と下部の座席は早見と詳細という異なる役割を持つため、同じ文字列でも許容する。

Hero直下には`公式情報`カードを常設し、公演の公式URLとSNSを表示する。値がない場合もカードや各欄を消さず`未登録`と示す。SNSは公演登録と対象編集で1行1リンクとして入力し、`ExperienceEvent.unitFieldsRaw`内の`socialLinks`へ複数値で保存する。同じ公演に紐づく各Visit詳細はこの公演情報を参照するため自動反映される。観劇回編集では公式情報を読み取り専用とし、対象へ書き戻さない。旧データは空配列として互換読込し、SwiftDataスキーマは増やさない。その直下へ`会場`カードを置き、会場名、住所、埋め込み地図をまとめる。行き方の説明は置かず、施設名と住所は長押しで選択・コピーでき、その下へカード幅いっぱいの横長Mapを置く。外部地図はApple MapsまたはGoogle Mapsを選択する。保存座標を優先し、次にPlaceMaster座標、住所または会場名のジオコードを使う。会場未登録時も空状態カードを残す。

観劇では上記を`作品・公演情報`へ拡張する。主催、企画・制作、運営、協賛は入力済み分だけ1列のラベル＋値で表示し、公式URL、チケットサイト、SNSは値がなくても`未登録`欄を残す。狭い2列組みは使わない。任意サブタイトルは同JSONの`eventSubtitle`へ保存する。その後に期限前の操作を集めた`次にやること`、会場名・住所・地図を置く。次にやることは公演日前は開き、当日以降は未完了がなければ閉じる。`作品・公演情報`は見出しのシェブロンで折りたためる。

編集とこの記録だけの削除は、全ジャンル共通で右上の`…`へまとめる。削除は確認ダイアログを経由する。

観劇のキャスト・スタッフ全体はメモ・感想と写真の後へ、公演側に保存した改行テキストとして表示する。その次へ、利用者が個別に登録した`お目当て・注目した人`だけを横スクロールの人物カードで表示する。注目人物は任意で、登録してもMY FAVOへ自動追加しない。旧形式の構造化キャストとその回のスナップショットは既存データに限って互換表示し、簡易編集から削除・再生成しない。

写真は`思い出 / チケット / グッズ`の3分類とする。空値・未知値を含む既存写真は思い出として扱い、利用者が編集して明示分類できる。Hero背景は各ジャンルの同梱イメージを既定とし、写真1枚目を自動採用しない。組み込み11ジャンルは、中央トリミングと下端フェード用余白を持つ電話向け縦長背景を各4枚用意し、写真編集内の横並びサムネイルから選択する。各ジャンルに明るい候補を1枚以上含め、おでかけ施設は`施設 / 動物園 / 水族館 / 温室`とする。編集画面で思い出写真を明示選択した場合はそちらを優先し、組み込み背景へいつでも戻せる。自作ジャンルはジャンル色グラデーション＋自分の写真を使う。アイキャッチに使っていない思い出写真は、1枚だけでも全幅へ広げず、3列の正方形aspect fillグリッドへ整列する。

観劇では4分類目として`ノベルティ・特典`を追加する。保存分類と費用計算ではグッズと特典を区別し、特典は金額・費用合計の対象にしない。一方、詳細表示は`写真・コレクション`1カードへまとめ、その中を`思い出`と`グッズ・ノベルティ・特典`の2まとまりで整列する。思い出・グッズ・特典は同カードの`追加`から写真ライブラリを開いて直接保存でき、カード全体を折りたためる。チケット写真は写真・コレクションへ混ぜず、`チケット・座席`へ表示する。感想も独立カードとして折りたためる。

登録・編集では各写真サムネイルから分類と付随情報を編集する。チケット／グッズでは同じ写真へVision OCRを実行し、OCR本文を画像に紐づけたまま保存する。本文から見つけた金額は候補として提示し、利用者が選んだ場合だけ画像別金額へ反映する。入力中の新規写真は`PendingPhoto.metadata`、既存写真は`PhotoMetadataDraft`に保持し、記録の保存確定前にSwiftDataモデルを書き換えない。思い出へ戻した写真はOCR本文・金額を保存時に空へ戻す。

費用合計は、確保済み以降の`TicketAttempt`にある券面＋手数料、グッズ写真の確認済み金額、遠征ToDoの確認済み金額を費目別に加算する。チケット写真の金額はTicketAttempt金額が0円の時だけ代替値として使い、両方は足さない。既存の`Visit.amount`は構造化明細がすべて0円の時だけ旧合計として使い、明細がある場合は参考値として保持して加算しない。観劇／ライブの記録詳細には`申込を追加`と`遠征ToDo`を置き、元Planがない過去記録では利用者がこの導線を押した時だけVisitへ紐づくPlanを作る。

遠征ToDoは既存の公演準備リストを拡張し、通常の準備に加えてホテル、新幹線、飛行機、現地交通、その他の遠征を選べる。遠征項目は実際の開始／終了日時、任意期限、費用、OCR原文、完了状態を持つ。画像OCRは原文だけを先に保持し、タイトル、日付、金額、種類は候補を利用者が押した場合だけ反映し、保存前には確定しない。通常準備の金額は費用合計へ入れず、遠征種別だけを遠征費として加算する。

## 8. 一覧・サマリー

一覧カードに出す情報は最大で以下に限定する。

- カバー写真
- タイトル
- ジャンル
- 日付
- 場所
- 評価または現在状態
- メモ1行

人物、金額、OCR、URL、天気は通常一覧に常設しない。期限や未整理など、次の操作に必要な状態だけ補助表示する。

## 9. 思い出の登録

下部タブバーはSwiftUI標準`TabView`を使い、`Home / 記録 / 追加 / カレンダー / 統計`の5項目をシステム標準の寸法で並べる。独自のカプセル、背景色、影、下端余白は追加せず、`.toolbarBackground(.ultraThinMaterial, for: .tabBar)`で透過ブラーを明示する。iOS 26では本文上へ浮く透過ブラーの標準タブバー、iOS 18では従来型の半透明タブバーとして表示する。各タブのルートは下端container Safe Areaを越えて描画し、本文とポスターをバーの背後まで続ける。スクロール末尾にはバー高相当の操作余白を持たせ、最後の要素をバーに隠さない。`追加`は独立画面へ切り替えず、標準タブ項目の選択をBindingで横取りして現在のタブを維持したまま表示する。内容は押した時点の表示中ジャンルUUIDからジャンル別定義を解決して固定する。観劇は専用3入口、Homeとその他ジャンルは通常4入口とし、ジャンル固有メニューを追加する場合も同じ定義へ集約する。Homeの上部ジャンルタップと本文横スワイプは同じジャンル遷移UUIDを正本とし、UUIDが入った時に追加文脈を有効化し、ジャンル画面を閉じてUUIDがnilになった時だけ総合Homeへ戻ったものとして解除する。Viewの`onAppear / onDisappear`は解除判定に使わない。FAVO・カレンダー・統計へ移ってもHomeのジャンル状態は保持し、`追加`押下時に現在の下部タブがHomeなら保持中ジャンル、Home以外なら通常メニューを選ぶ。

Home本文のジャンル横スワイプは縦スクロール用Panとの同時認識だけを許可し、Ticket Scheduleカード等のタップ・長押し操作とは排他にする。横パン成立時はカードの選択状態を破棄し、ジャンル遷移先UUIDが有効な間は開始地点の進捗管理シートを表示しない。短いタップは従来どおりカード操作、縦移動はスクロールとして扱う。

1. `予定を立てる`: これから体験するPlanを登録
2. `体験済みを記録`: 予定を経由しない正式なVisitを直接登録
3. `クイック登録`: 通常と同じExperienceEventを、ジャンル/タイトル必須、アイキャッチ/URL/読み取りメモ/短いメモ任意の簡易フォームで`気になる`として保存
4. `チケットスケジュールを追加`: 対象イベントを検索または最小作成し、そのPlanへ抽選/発売スケジュールを紐付ける

初期状態では`予定を立てる`を既定の追加方法とする。利用者が設定で既定値を変更できるようにしても、入口の並び順自体は動かさない。観劇、ライブ、美術展、おでかけなどは予定起点を基本とし、御朱印、お酒、過去記録の移行などは`体験済みを記録`から直接登録できる。

`予定を立てる`と`体験済みを記録`は対象情報と基本入力部品を共用するが、初期状態を分ける。

| 入口 | 初期表示する情報 | 初期状態で閉じる情報 | 主な表示先 |
|---|---|---|---|
| 予定を立てる | 対象、日時、会場、通知、チケット | 写真、感想、評価 | 次の予定、カレンダー、要対応 |
| 体験済みを記録 | 対象、体験日、写真、感想、評価 | 通知、抽選、発売 | 最近の思い出、記録一覧、統計 |

`予定を立てる`は最初に予定の対象を確定する。主操作は`新しく登録`とし、同じフォーム内でExperienceEventとPlanをまとめて保存する。補助操作の`気になるから選ぶ`では、`気になる`状態の既存ExperienceEventを検索して選び、その対象へ新しいPlanを追加する。検索対象はタイトル、シリーズ、ジャンルとし、文字幅・大文字小文字・空白の差を無視する。選択一覧はSwiftData実体を保持せず、タイトル・ジャンル・シリーズ・アイコン・UUIDの表示用スナップショットを使い、シート終了後にUUIDを親フォームへ反映する。選択一覧と選択後カードのタイトルは15pt基準の1行とし、長文は縮小する。選択後は対象名、ジャンル、公式URLを予定フォームへ引き継ぐ。対象詳細から開いた場合は選択を省略し、表示中のExperienceEventへ直接追加する。

予定日時はアプリ全体で日本語の年月日表示とする。チケット工程日時は日付と15ptの時刻を横並びにし、時刻は`00:00〜23:55`を5分刻みの単一列ホイールで選ぶ。観劇とライブの`体験日程`だけは日付を1件指定し、その下へ同じ日付の`開場 → 開演 → 終了`を時刻のみで並べ、時・分を一体にした10分刻みホイールで選ぶ。開場は初期状態を`--:--`の未設定とし、時刻選択時の初期候補を開演30分前にする。行末の×で未設定へ戻せる。開演を変更した時はそれまでの開演〜終了の長さを維持して終了も同じ差分だけ動かし、終了が開演より前にならないよう補正する。開場設定済みなら開演との時間差を保って同じ差分だけ動かす。同じ日付を越える値は当日内へ収める。映画、美術展、おでかけ、書籍、酒、御朱印などには開場を表示・保存しない。観劇/ライブのPlanを外部カレンダーへ追加または片方向更新する時は、開場が設定済みなら開場、未設定なら開演をカレンダー上の開始時刻とし、終了はPlanの終了時刻を使う。

Planは必ずExperienceEventへ紐付け、Planだけを単独保存しない。予定日時を持たない`気になる`登録はExperienceEventだけで成立するが、利用者自身の候補日または確定日を入力した時点でPlanを作る。開催日・開催期間など対象そのものの日付と、利用者が行く予定日時は区別する。Plan作成後は対象の`気になる`状態を通常状態へ移す。

予定日を迎えたPlanから`記録を残す`を実行した場合は、対象、日時、会場、チケット参照を引き継いでVisit作成を開始する。元のPlanとTicketAttemptは削除せず完了状態として保持し、同じExperienceEventへVisitを紐付ける。

`チケットスケジュールを追加`は`新規イベント / 登録済みイベント`の2入口とし、チケットだけを独立保存しない。新規イベントではExperienceEvent、Plan、TicketAttemptを同じ保存処理内で作成する。登録済みイベントではタイトル、シリーズ、ジャンルからExperienceEventを検索し、そのイベントに未完了Planがあれば紐付け先を選んでTicketAttemptだけを追加する。文字幅・大文字小文字・空白の差は検索時に無視する。

登録済みイベントにPlanがない場合、または同じイベントに別の予定を作る場合は、選択したExperienceEventへPlanとTicketAttemptをまとめて追加する。既存Planを選んだ後は予定名、日時、会場を参照表示とし、チケット保存によってPlanの日時・会場・公式URLを上書きしない。通常は予定詳細から追加し、下部の`追加`タブはイベント詳細まで移動せず登録するための短縮導線として扱う。

登録済みイベントのPlan選択は`追加先予定｜タイトル / 日時 / 会場`を13pt基準の1行メニューで表示し、長文は1行内で縮小する。選択後の基本情報は`ジャンル / タイトル / 日時 / 会場`を12pt・上下7ptのコンパクトな1カードへまとめ、項目名と値を各1行に固定する。取得済みの値は`primary`で表示し、文字どおりの`未設定`だけを補助色にしてプレースホルダーと区別する。チケット情報内の固定`入力内容｜チケットスケジュール`は登録内容と重複するため置かない。登録内容に応じて切り替わる説明は、情報アイコン、`この登録で入力する項目`見出し、淡いアクセント背景を持つ案内カードとして表示し、未入力フォームのプレースホルダーと視覚的に分離する。説明本文は11ptの1行に固定し、小幅時は68%まで縮小して折り返さない。予定情報とチケット情報の間には、既存Planを選択済みでも`画像からチケット情報を読み取る`行を表示する。読取中は同じ行へProgressを表示し、完了後は検出した候補項目を確認ダイアログで示してから反映する。現在の登録内容で非表示の項目と既存Planのタイトル・日時・会場は候補から除外する。文字入力は名義、購入先、購入URL、金額、座席、タグ、メモを含めて上段項目名／下段入力欄の二段構成とする。

抽選フローの日付入力は`抽選申込開始 / 抽選申込締切 / 当落発表 / 入金締切`とし、先着フローの`発売開始`と名称だけで区別できるようにする。チケット単体編集と予定・チケット一括手配で同じ工程名を使用し、日付順エラーも同じ呼称へ揃える。一括手配の工程名は13ptへ抑える。日程未定時の説明は`未定の予定はComing Up・カレンダーに表示されません`へ短縮し、10pt・最大2行で省略せず表示する。

`クイック登録`はPlanやVisitを作らず、通常と同じExperienceEventを`気になる`状態で作る入力項目限定版とする。別のInboxItemへ保存して後から変換する工程は設けない。URLとOCRは独立した登録方式ではなく、同じ画面内でタイトルと画像を埋める入力補助とする。URLから取得したタイトルは候補として表示する。カメラ/写真のOCRは先頭行を使わず、認識行の文字領域の高さ・面積・認識確度を基に、大きく目立つ文字をタイトル候補として順位付けする。1位が十分に目立ち、同じ領域の文字認識にも曖昧さがない場合だけ候補表示し、利用者が`タイトルに使う`を選んだ時だけ反映する。確定できない場合は自動候補を出さず、目立つ領域のVision上位候補とOCR全行を提示して利用者に選ばせる。OCR全文は選択・コピー可能な確認欄へ残し、対象の`読み取りメモ`へ保存する。OCRに使った画像は対象のアイキャッチ候補にする。後から同じ対象へPlanまたはVisitを追加し、その時点で状態を通常へ移す。

クイック登録後も対象詳細の`対象情報・画像を編集`からアイキャッチを差し替え・解除できる。画像は読み込み時に長辺1600px以内、JPEG品質85%へ圧縮し、元画像のメタデータを保持しない。

登録フロー:

1. `体験済みを記録`を選択
2. コンパクトなジャンルメニューでジャンルを選択。全ジャンル一覧を常時展開しない
3. 主操作の「新しい作品・対象を登録」、または補助操作の「登録済み作品・対象に記録を追加」を選択
4. 既存対象の場合は検索結果から対象を選び、対象側情報を参照して今回固有のVisitだけを入力
5. 新規対象の場合は対象名だけで作成可能とし、必要に応じてシリーズを検索・連結
6. 記録の必須情報を入力
7. 必要な補助ユニットだけ展開
8. 保存後に記録詳細を開く

下部の`追加`タブの`体験済みを記録`では、ジャンルをコンパクトなメニューで選んだあと、主操作の`新しい作品・対象を登録`と、補助操作の`登録済み作品・対象に記録を追加`を同じ選択画面に表示する。既存対象は選択ジャンル内をタイトルまたはシリーズ名で検索でき、未入力時は最近更新した対象を少数だけ表示する。

登録済み対象の候補部分は画面全体とは別の縦スクロール領域にし、候補が増えても下部説明や閉じる操作へ到達できる高さに制限する。右端にはスクロールインジケータを表示する。

予定詳細の`参加記録を入力`はVisitを即時作成せず、同じ記録入力フォームを開く。対象、予定日時、終了日時、会場、座席、金額、メモ、公式URL/購入URLを初期値として引き継ぎ、利用者が確認・修正して保存した時だけVisitを作成する。保存成功と同じ処理内でPlanを参加済みにし、代表のTicketAttemptも参加済みに更新する。入力をキャンセルした場合はPlanとTicketAttemptを変更しない。

入口の情報優先順位:

```text
ジャンル [観劇 ▼]

[ ＋ 新しい作品・対象を登録 ]

または

登録済み作品・対象に記録を追加
[ タイトルを検索 ]
```

「既存対象への記録追加」と「シリーズ連結」は別機能とする。既存対象への追加は再鑑賞、再訪、再読、同じ銘柄の再記録に使う。シリーズ連結は新しい対象を別作品として作成したうえで任意の親シリーズへ紐付ける。

既存対象を選んだ場合に参照する対象側情報:

- タイトル、ジャンル、シリーズ、代表写真
- 基本の人物・団体、公式URL、対象種別

Visitへ入力する今回固有の情報:

- 体験日、会場、当日の人物・団体
- 写真、評価、座席、金額、感想、天気

対象情報をVisitへ無条件コピーせず、対象はリレーション参照、当日固有情報はVisitのスナップショットとして保存する。

常時表示:

- ジャンル
- 対象名
- 日付
- 場所
- 評価または状態
- メモ

アコーディオン:

- 人物・団体
- 写真
- チケット・座席
- OCR・取込
- 金額
- 公式情報
- 詳細オプション

新規対象を作る場合でも、対象側の詳細情報をすべて同時入力させない。まず名称だけで対象を作成でき、対象詳細から後で補完できる。

## 10. 編集画面

編集画面は次の3種類へ分離する。

### 対象を編集

- 対象名
- シリーズ、サブ種別
- 代表写真
- 基本となる人物・団体
- 公式情報
- 対象メモ

### 記録を編集

- 日付、場所
- 評価、状態
- 当日の人物・団体
- 写真、OCR
- 座席、金額、天気
- 感想、詳細オプション

対象名や対象の公式情報を変更しない。

### 予定・チケットを編集

- 開催日時、開場、会場
- 会場名は手入力でき、Apple Maps選択は同じ予定の会場名・住所・座標を補完する
- 予定固有の公式ページは`この予定の公式URL`と明示し、申込・購入URLと区別する
- 抽選または発売・予約のスケジュール
- 取得後のチケットメモ、リンク、任意のQR・画像
- 発券・表示確認と任意の確認リマインダー
- 予定メモ

公演名、サブタイトル、会場、住所、予定公式URLは入力後も項目名が残る明示ラベルを使う。Planだけを編集する時は申込タグを表示しないため、自由記述欄を`予定メモ`と表記する。体験済みの感想や写真を変更しない。参加後はPlanからVisit作成へ遷移する。

## 11. 御朱印専用登録フロー

御朱印は一般的な対象選択より先に、登録先の御朱印帳を選ぶ。

```text
御朱印を登録
↓
使用中の御朱印帳を選択
↓
新しい寺社 / 登録済み寺社を選択
↓
御朱印を入力
↓
保存して完了 / 保存して同じ寺社で続けて登録
```

御朱印帳選択画面:

- 使用中の御朱印帳を表紙画像、名称、登録済み御朱印件数で表示
- 同じ利用者が寺社や用途ごとに複数冊を並行利用できる
- `新しい御朱印帳を登録`を補助操作として同じ画面へ置く
- 閉じた御朱印帳は新規追加候補に出さないが、過去帳一覧から閲覧・編集できる

御朱印帳の管理:

- ページ数、総ページ容量、使用ページ数を管理しない
- 裏面利用、見開き、書置きなどの差を自動判定しない
- 満了の自動判定、自動クローズを行わない
- 利用者の明示操作だけで御朱印帳を閉じる
- 閉じた御朱印帳は新規御朱印を追加できないが、既存御朱印と帳面情報は編集できる
- 御朱印帳には登録済み御朱印の件数を表示する

寺社選択:

```text
[ ＋ 新しい寺社の御朱印を登録 ]

または

登録済み寺社から選ぶ
[ 寺社名を検索 ]
```

御朱印登録項目:

- 御朱印画像/アイキャッチ: 任意
- 寺社名: 必須
- よみ: PlaceMasterの読みを寺社名上部へ薄いルビとして表示
- 住所、ミニMap、公式URL、御祭神: 任意。登録済み寺社から引継可能
- 参拝・拝受日時、写真、同行者、メモ: 任意
- 御朱印名/種類、通常/限定、直書き/書置き: 任意
- 初穂料・納経料: 任意。`300円 / 500円 / その他`をクイック選択し、その他は任意金額を入力

保存操作:

- `保存して完了`
- `保存して同じ寺社で続けて登録`

続けて登録では御朱印帳、寺社、日時、同行者を保持する。御朱印画像、御朱印名/種類、金額、写真、メモは次の記録へ引き継がない。

御朱印帳の基本情報:

- 表紙画像
- 御朱印帳名、または購入した寺社
- `神社用 / お寺用 / 共用`
- 使い始めた日
- メモ

御朱印帳は長押しから編集、閉じる/再開、削除へ進める。削除は帳面内の御朱印と写真を含むため、対象件数表示、バックアップ案内、`削除する`の確認入力を含む3段階確認にする。削除しても寺社マスターと別の御朱印帳は削除しない。

## 12. 共通入力ルール

### OCR・URL取込

OCRとURL取込は保存操作ではなく入力補助とする。

1. 画像、URL、貼り付けテキストを読み取る
2. タイトル、日付、会場、人物、金額、ISBNなどの反映先を候補表示する
3. 利用者が候補ごとに反映先変更、編集、除外を行う
4. `反映する`を押した項目だけフォームへ入れる

- 信頼度が低い候補は要確認表示にする
- 既存入力を自動上書きしない
- OCR全文を確認画面へ残し、選択、コピー、貼り直しを可能にする
- 公演系は登録画面上部に`公式サイトから情報を取得`と`チラシ・チケットをOCRで読み取る`を常設する
- 本はISBNバーコードを優先し、奥付OCRを予備経路にする。ISBNは任意の内部識別情報として保持し、通常入力で手入力を要求しない

### 人物・団体の重複候補

人物名の検索用文字列は、Unicode正規化後に半角/全角スペース、連続空白、改行、タブを除去し、英字の大文字/小文字を無視する。

```text
山田 太郎
山田　太郎
山田太郎
```

上記は同一候補として提示する。さらに表示名・よみ・別名のいずれからでも検索できるようにし、人物の検索・登録画面では汎用の入力補助設定にかかわらず候補を常時表示する。候補選択または完全一致入力では既存人物を再利用し、部分一致や同姓同名は候補から利用者が判断する。この規則は人物マスター管理、FAVO、著者、俳優、アーティスト、監督、作家などPersonMasterを使う全ジャンルへ適用する。

### SNSタグ

全ジャンルの対象と記録に、複数登録可能な任意のSNSタグを持たせる。

- `#`なしの入力は表示時に補完する
- 全角/半角スペースを無視して重複候補を出す
- URL/OCRから公式タグ候補を提案する
- タグ群をまとめてコピーできる
- 通常は`公式情報`アコーディオン内へ置く

### 日付

期間は終了日を必須にしない。開始日だけで保存でき、終了日がない対象を無期限の`開催中`とは自動判定しない。

### 写真と写真に紐づく情報

写真を用途別に二重保存しない。共通写真へ任意の作品情報または体験情報を付け、詳細画面で意味のあるまとまりとして再構成する。

- 美術展: 作品名、作家名、メモ
- おでかけ: 名前、種類、メモ、任意の金額
- 写真がない場合はテキストだけの作品/体験メモを追加できる
- 情報付き写真も通常の写真ギャラリーへ含める

### 評価

映画と本は、記録回ごとに`1.0...5.0`を0.5刻みで評価できる。ジャンルごとに機能ON/OFF可能、初期値はOFF、未評価保存を許可する。OFFにしても保存済み評価は削除しない。一覧への評価表示は別設定とする。

## 13. ジャンル別登録フロー

### 映画

```text
映画を記録
├ 新しい映画を登録
└ 登録済み映画に鑑賞記録を追加 [検索]
```

映画情報:

- タイトル必須
- 読み、原題、ポスター、公開年、監督、出演者、制作会社、公式URLは任意
- 1作品は複数のコレクションへ所属できる
- コレクションは代表画像、作品数、作品一覧、公開順/時系列/任意順を持つ

鑑賞記録:

- 鑑賞予定/鑑賞済み、鑑賞日、映画館、上映形式、同行者、写真/半券/OCR、金額、感想/メモ、任意評価
- 再鑑賞は映画情報を参照し、新しい鑑賞記録だけを追加する

映画トップはコレクションカードを横スクロール表示し、`一覧を表示`からコレクション一覧へ進む。主要都市のシネコンとミニシアターをPlaceMasterの初期候補へ収録する。

### 観劇

通常利用は`公演 -> 観劇回`の2層とし、作品/演目の理解を登録時に要求しない。再演をまとめる機能は内部拡張余地を残し、必要な利用者だけ後から使えるようにする。

公演情報:

- 公演名必須
- メイン画像/チラシ、公演種別、劇団/主催、公式URLは任意
- 人物・団体は閉じたアコーディオンに置き、原作/原案、作/脚本、演出、出演者、音楽、振付などを役割付きで複数登録できる
- 公演名と主催が近い既存公演がある場合は、情報を引き継ぐか新規作成するか確認する。自動統合しない
- チラシ/ポスターは公演、当日の写真/半券は観劇回へ保存する

観劇回:

- 観劇日時、会場、座席自由入力、当日の出演者差分、写真/半券、金額、同行者、感想/メモ
- 開場時刻は予定/チケット側、上演時間は公演側の任意項目、休憩は標準項目にしない
- 感情タグ初期候補は`また観たい / 泣いた / 笑った / 驚いた / 圧倒された / 考えさせられた / 音楽が好き / 演出が好き / 衣装・美術が好き`

公演種別初期候補:

- 演劇、2.5次元舞台、ミュージカル、歌舞伎、落語・寄席、ダンス・バレエ、オペラ、その他
- 2.5次元舞台は観劇の独立した公演種別とし、演劇・ミュージカルのどちらかへ強制的に寄せない
- コンサート／ライブは別のLIVEテンプレで扱い、アイスショー、マジック、サーカス、能、狂言、講談、浪曲、朗読劇等は`その他`から具体名を入力する
- `その他`を選んだ場合は`その他`という文字を保存せず、入力した具体名を表示・検索・統計に使う。固定候補は安定キー、具体名は後方互換JSONに保持する
- 公演種別は公演である`ExperienceEvent.subTypeKey`、鑑賞方法は観劇回である`Visit.unitFieldsRaw.styleNames`に分離する。鑑賞方法の初期候補は`現地 / 配信 / ライブビューイング`
- 将来カスタム候補の追加・並べ替え・非表示・統合を実装する場合も、使用済みの安定キーと具体名を破壊しない

一覧サマリーはチラシ、公演名、観劇日、劇場、予定/観劇済みに限定する。

### ライブ

通常利用は`ライブ・イベント -> 参加回`の2層とし、入口はアーティストではなくライブ名から始める。

```text
ライブを記録
├ 新しいライブを登録
└ 登録済みライブに参加記録を追加 [検索]
```

ライブ情報:

- ライブ名必須
- 種別は`ワンマン / 対バン / フェスティバル / ライブイベント / 配信ライブ / その他`
- アーティストは最初から表示して複数登録可能。役割は`メイン / 出演 / ゲスト / オープニングアクト`を任意設定する
- ツアーは種別にせず、必要な場合だけ関連ライブとして後からまとめる

参加回:

- 参加日時、会場、写真/アイキャッチ、感想/メモを基本とする
- 座席/整理番号、当日のゲスト、セットリスト、同行者、金額、公式情報は閉じた任意項目
- セットリストは曲名と順番を持ち、まとめて貼り付け可能。アンコールは区切りで表現する
- フェスは出演者全員と実際に観たアーティストを分け、詳細ON時だけアーティスト別セットリスト、ステージ、時間帯を記録する
- 配信ライブは会場の代わりに配信サービスと視聴URLを使う
- 感情タグ初期候補は`また行きたい / 泣いた / 笑った / 驚いた / 圧倒された / 盛り上がった / 音が良かった / 演出が好き / セットリストが好き`

ライブトップは今後のライブ、アーティスト、参加したライブ、データがある場合だけツアー/関連ライブを表示する。

### 本

通常利用は`本 -> 読書記録`の2層とする。

```text
本を記録
├ 新しい本を登録
└ 登録済みの本に読書記録を追加 [タイトル・著者検索]
```

本の登録はISBNバーコード、表紙/奥付OCR、タイトル入力から開始できる。本の情報は表紙、タイトル必須、著者、巻数、出版社、発売日、ISBN、公式/参考URLを持つ。

- 1冊は複数のシリーズ/コレクションへ所属できる
- ISBNが異なる単行本、文庫、電子版を同じシリーズへ入れられる
- 状態は`気になる / 積読 / 読書中 / 読了 / 中断`
- クイック登録は気になる、開始日あり/読了日なしは読書中、読了日ありは読了を自動候補にする
- 積読と中断は利用者が選択し、自動判定後も手動修正できる
- 読書記録は開始日、読了日、`紙 / 電子 / オーディオ`、感想/メモ、写真、任意評価を持つ
- 再読は同じ本へ新しい読書記録を追加する
- 保管場所、貸出状況、読書タイマー、読書速度、毎日のページ進捗は扱わない

本の種別初期候補は`小説 / エッセイ / ノンフィクション / ビジネス・経済 / 実用・趣味 / 人文・思想 / 科学・技術 / 漫画 / 絵本 / 詩歌 / 雑誌 / その他`とし、カスタム管理可能にする。印象タグ初期候補は`また読みたい / 心に残った / 泣いた / 笑った / 驚いた / 考えさせられた / 学びがあった / 読みやすかった`とする。引用は閉じた任意項目で、本文、ページ、メモを複数登録できる。

本トップは読書中、気になる/積読、最近読んだ本、シリーズ/コレクションを表示する。

### 美術展・展示会

通常利用は`展覧会 -> 鑑賞記録`の2層とする。同じ巡回展は展覧会を共通化し、会場と鑑賞日は鑑賞記録へ持たせる。会場ごとに内容やタイトルが大きく違う場合だけ別展覧会として関連付ける。

展覧会情報:

- 展覧会名必須
- メイン画像/チラシ、開催開始日、任意の終了日、作家/アーティスト、主催/企画、公式URL、SNSタグ
- 排他的な単一種別を使わず、`現代美術 / 絵画 / 彫刻 / 写真 / 映像 / インスタレーション / メディアアート / 建築 / デザイン / 工芸 / ファッション / 漫画・アニメ / 歴史・文化 / 科学・自然`などを複数選択できるジャンル・テーマとして扱う

鑑賞記録:

- 鑑賞日、会場、写真、同行者、金額、感想/メモ、印象タグ
- 写真へ作品名、作家名、メモを付けると`印象に残った作品`として自動表示する
- 写真がない作品はテキストだけで記録できる
- 印象タグ初期候補は`また行きたい / 心に残った / 驚いた / 圧倒された / 考えさせられた / 展示空間が好き / 作品構成が好き`

展覧会トップは開催中/これから行きたい展覧会、チケットスケジュール最大2行、最近観た展覧会、アーティスト、美術館/ギャラリー、写真ギャラリーを表示する。

### おでかけ施設

```text
おでかけを記録
↓
テーマパーク / 動物園 / 水族館 / 植物園 / その他
↓
お気に入り、最近使った施設、全施設から選択 / 新しい施設を登録
↓
訪問記録を入力
```

施設情報は施設名必須、種別、メイン画像、読み、住所/Map、公式URL、SNSタグ、お気に入りを持つ。訪問記録は訪問日、写真/アイキャッチ、同行者、チケット/予約、金額、天気、体験、感想/メモ、SNSタグを持つ。

体験情報:

- 共通写真へ体験名、種類、メモ、任意金額を付ける。写真なしでも登録可能
- テーマパークの初期候補は`アトラクション / ショー・パレード / グリーティング / イベント / フード・ドリンク / グッズ・購入品 / その他`
- 動物園、水族館、植物園では表示候補を動物、生き物、植物、季節展示、餌やり/体験などへ変える
- 体験種類はカスタム追加、並び替え、非表示、統合可能

おでかけトップはこれからのおでかけ、チケットスケジュール最大2行、お気に入り施設、最近行った施設、写真ギャラリーを表示する。

### お酒

```text
お酒を記録
├ 新しいお酒を登録
└ 登録済みのお酒に飲酒記録を追加 [検索]
```

- 初期種別は日本酒、ワイン、ビール、ウイスキー、焼酎、その他
- 既存対象は種別、銘柄、メーカー/蔵、産地、ラベル、仕様、公式URLを参照する
- 飲酒記録は日付、場所、写真、飲み方、金額、同行者、感想/メモを持つ
- 軽い印象は`また飲みたい / 好み / ふつう / 合わなかった`の単一選択、未選択可
- 香り、甘辛、軽重、酸味、余韻、飲みやすさ等の詳細評価は機能ON時だけ表示し、酒種別でラベルを変える

## 14. チケット共通フロー

チケット登録の入口は次の3択とする。

```text
チケットを登録
├ 抽選に申し込む
├ 発売を待つ
└ チケットを取得済み
```

抽選:

- `申込枠`は`FC先行 / 公式先行 / プレイガイド先行 / カード枠 / 一般抽選 / その他`
- `申込アカウント（任意）`は設定済みFC・プレイガイド等を`サービス名｜名義`で表示し、選択すると購入先・名義・登録URLを自動入力する
- 同じFCの複数名義は1名義1アカウントとして別候補にし、名義未設定とアカウント未選択も許容する
- アカウント未選択時は購入先・名義・URLを手入力できる
- 申込開始、申込締切、当落発表、入金期限、発券開始、発券/受取期限を、分かる項目だけ登録する
- 申込URLからプレイガイドを自動候補にし、URLがない場合だけプレイガイドの一般入口を利用できる

発売:

- `販売方法`は`先行販売（先着） / 一般発売 / リセール / その他`
- 発売開始、購入先、申込・購入URLを表示し、名義・アカウントは表示しない
- 同じ公演の先行販売と一般発売は別々のTicketAttemptとして登録でき、それぞれ発売アラートを持つ

取得済み:

- 購入先、金額、枚数、座席、タグ・メモを表示する
- 申込枠・販売方法・名義は表示しないが、抽選から継続した既存値は削除しない
- `購入先`はプレイガイドに限定せず、FC・公式サイト・その他の購入サイトを手入力できる
- 画像/PDF、発券・表示確認、確認リマインダーは任意
- `チケットQR・画像`は通常写真と分け、年間まとめ、写真ギャラリー、共有画像へ出さない
- QR全画面表示中は明るさと自動ロックを入場確認向けに調整する
- 動的QRやスクリーンショット無効の場合があるため、公式サイト/アプリ表示を優先する注意を出す

開催日時と会場は公演/展覧会/予定側を参照し、チケットへ重複保存しない。ジャンルトップのチケットスケジュールは共通チケットデータから直近1〜2件を表示し、登録がなければセクションごと隠す。

## 15. 場所・種別・タグのマスターUI

会場入力は利用者へ施設/ホールの階層選択を要求せず、1つの検索/自由入力欄にする。

```text
会場 [東京芸術劇場 コンサートホール]
```

- 施設名だけでも、ホール名込みでも保存可能
- 住所がある場合は名称検索ではなく住所を優先してMap表示する
- 主要都市圏の映画館、劇場/ホール、美術館/博物館/ギャラリー、おでかけ施設を初期候補として収録する
- 内部で親施設を共有しても、通常登録では細かな施設階層を見せない
- 場所マスターは`未設定 / 営業中 / 閉館・閉園`の営業状態を持つ。管理一覧では赤い閉館バッジと状態フィルター、編集では状態選択を提供する
- 閉館施設はFAVOと過去記録の登録済み場所候補で`閉館`を明示した上で利用できるが、未来予定の通常候補からは除外する

カスタム種別と印象タグは、名前、アイコン、色、並び順、表示/非表示を管理できる。未使用のカスタム項目は削除可能、使用済み項目は削除せず別項目へ統合または非表示にする。初期項目は削除せず非表示のみ可能とする。

## 16. 実装移行方針

1. 既存モデルは維持し、まずViewの責務を分離する。
2. `EditExperienceView`が同時更新しているExperienceEvent/Visitを、対象編集と記録編集へ分割する。観劇は分割済み。ほかのテンプレートは観劇の完成後に共通完成条件を抽出して適用する。
3. `AddExperienceView`の先頭に既存対象検索を追加し、新規対象作成と既存対象へのVisit追加を同じ入口から選べるようにする。
4. `CategoryTopView`は対象ライブラリを主役に再構成する。
5. `ExperienceDetailView`と`EventDetailView`を閲覧専用として整理する。
6. View分割後にデザインラフを各画面へ適用する。

## 17. 完成条件

- どの情報がExperienceEvent、Visit、Plan、TicketAttemptのどれに属するか画面上で判断できる
- 一覧から編集コントロールが排除されている
- 対象編集で過去Visitの固有情報が変わらない
- 記録編集で対象名や対象公式情報が意図せず変わらない
- Plan編集とVisit編集が混在しない
- 登録は必須情報だけで保存でき、詳細入力は任意展開できる

## 18. 設定の情報設計

設定トップは機能を直接並べず、次の5分類から各設定へ移動するハブとする。各行には設定できる内容を短いマイクロテキストで表示する。

```text
設定
├ マイ・登録情報
│  ├ プロフィール
│  └ 登録情報・連携
├ アプリ設定
│  ├ 表示・外観
│  ├ ジャンル
│  ├ 記録・入力補助
│  └ 通知
├ データと同期
│  ├ データ管理
│  └ 同期・バックアップ
├ プラン
└ サポート
```

「アプリ設定」のマイクロテキストは次を正本とする。

- 表示・外観: Home表示、外観、文字、フォント、テーマ
- ジャンル: 表示順、表示・非表示、自作ジャンル、有効ユニット
- 記録・入力補助: 初期値、写真圧縮、URL・OCR・Map・天気の補助
- 通知: チケット期限、公演前日・当日、会員期限、レポート

初回ジャンル選択のやり直しは「アプリ設定 > ジャンル」の末尾に置き、既存記録を削除しないことを明記する。テスト権利、仮データ、通知診断、全データ削除テストはDEBUGビルドだけの「開発者メニュー」へ分離し、通常利用者へ表示しない。

「記録・入力補助」には現在動作する設定だけを置く。記録日は今日を初期値として入力画面で変更できるため、単一選択肢の設定は置かない。Apple Musicなど未実装の将来候補も通常設定へ予告表示しない。

「表示・外観」のHome表示には、現在内容を表示できるセクションだけを置く。写真付き記録の横スクロールは「最近の思い出」と呼ぶ。お気に入り/ベストは集計元が実装されるまでHomeにも設定にも予告枠を出さない。

## FAVOプロフィール編集とHero

MY FAVO編集は、人物・団体／キャスト、作品／体験、場所の3群を同じ検索画面で提示する。人物は登録済みマスターから選ぶほか、新規登録で人物マスター・FAVOプロフィール・MY FAVO固定を一度に作る。固定済み行のタップとFAVO詳細右上の鉛筆からプロフィール編集へ進む。

編集Formは次の責務を分ける。

- `基本情報・全体へ反映`: 正式名、よみ／シリーズ／都道府県、役割／住所、公式URL。保存するとマスター管理、キャスト、候補表示に反映する
- `FAVOでの表示`: 呼び名、FAVOカラー、推し始めた日、きっかけ、個人メモ。対象のFAVO詳細だけに反映する
- `FAVO写真`: 16:9のHero写真1枚と正方形アイコン1枚。写真選択中はDraftへ保持し、保存時だけモデルへ反映する

FAVO詳細HeroはFAVO専用Hero写真を最優先し、未設定なら人物マスター画像または作品アイキャッチ、それもなければFAVO色グラデーションを表示する。アイコンはFAVO専用、マスター画像、種別アイコンの順で戻る。

Heroと次の予定の後へ`推しギャラリー`を置き、3列で最大6枚を先行表示する。写真詳細は全プランで閲覧可能とする。Pro／Premiumは一度に20枚まで追加でき、保存時に長辺1600px以内へ縮小する。管理画面では任意の日付・メモ、プロフィールごとに1枚のお気に入り、ドラッグ並べ替え、削除を提供する。無料プランへ戻った場合も保存済み写真は隠さず、追加・編集・並べ替え・削除だけをロックする。

Heroと次の予定の後、推しギャラリーの前へ`記念日`を置く。人物・作品／体験・場所の全種別で、名称と起点日を複数登録できる。詳細は次回が近い順に3件まで表示し、起点日、次回の月日、残り日数、1年以上経過している場合の周年を示す。管理画面では追加、編集、ドラッグ並べ替え、確認付き削除を提供する。記念日は全プラン共通とし、通知は保存基盤とは分けて後続実装する。2月29日は2月28日や3月1日へ暗黙補正せず、次の閏年を次回とする。

### 観劇公演の同一性とアイキャッチ

同一ジャンルの同名公演は、新規記録・公演登録・予定登録で既存公演マスターを再利用する。空白・全半角・大文字小文字だけが異なる保存済み公演もProductionsでは1作品として扱い、予定・申込を持たない純粋な興味あり公演はInterestsだけへ表示してProductionsへ二重表示しない。公演登録で選んだアイキャッチはB判比率へ中央トリミングして保存し、一覧は保存済みアイキャッチまたは代表写真を固定枠の中央aspect fillで表示する。

### 観劇の登録系フォーム

公演登録、公演情報編集、公演地編集、観劇予定、チケット手配、観劇記録追加・編集の通常入力は、上段16ptの項目名と下段16ptの入力欄を持つ66pt基準行へ統一する。項目名と上枠・区切り線の間に9pt、行下に8ptを確保する。`任意 / 必須`は項目名の隣、プレースホルダーは下段へ置く。文字入力は値がある時だけ右端へ×丸を表示し、その項目を全文消去できる。プルダウンの選択値は16ptで右寄せし、右端から4pt追加で離す。メモは5行分、座席は通常1行・長文時のみ最大2行とする。公演登録と公演情報編集の`公演基本情報`は初期展開し、`会期・会場`と同じカード見出しと標準矢印で開閉できる。観劇記録では公演名・シリーズ、日付・時刻・鑑賞方法・会場・住所・評価、チケット状態・座席、金額、感想メモ、公式URL・SNS、OCR原文、人物名、自由感情タグも同じ構成にし、写真・人物候補・感情タグ候補のカード/グリッドは既存構造を維持する。

公演種別は未選択時も`未設定`を表示し、下段全体を選択Menuのタップ範囲とする。SNSは共通16pt項目名と上下6pt余白を使い、各サービスを28ptアイコンと`X / Instagram / Threads`の英字名を併記したカプセルで示す。公演地の会場名は2文字以上の入力を450ms待ってApple Maps候補を最大3件表示し、候補選択で会場名と住所を同時反映する。会場名と住所が揃った時は会期入力の下へ170pt高のMapプレビューを表示し、会場名と住所を組み合わせて位置を解決する。

共通セクション名は16pt Semibold、通常項目名は16pt Semibold・`primary 78%`とする。入力本文は16ptを基準にし、住所とURLだけは長文を見通しやすくするため入力値・プレースホルダーを14ptとする。Apple Maps候補は会場名の完全一致、前方一致、部分一致の順で表示し、文字一致する候補がある時は関連施設だけの候補を混ぜない。

公演地名、会場、住所、会期、開始日、終了日の項目名はすべて16pt Semibold、自由入力値とプレースホルダーは16pt Regularへ揃える。compact DatePickerだけは標準17pt相当から15pt相当へ縮小する。

観劇予定追加・編集の`体験日程`では、共通日付と`開場 / 開演 / 終了`を別行にする。日付と各時刻は二段構成の下段右へ置き、時刻は`00:00〜23:50`を`HH:mm`の1項目として並べる単一列ホイールで10分刻みに選ぶ。時と分を別列にはしない。予定対象の選択済み公演タイトルは13pt Semibold、`Apple Mapsで会場・住所を入力`は13pt Semiboldの1行とし、狭幅時だけ78%まで縮小する。

### 観劇公演登録の共通入力フォーム

URLから登録と画像から登録は別の登録結果画面を持たず、同じ`公演名 / 公式URL / 公演ビジュアル / 公演地名 / 会場 / 住所 / 開催期間`フォームへ、取得できた項目だけを反映する。取れない項目は空欄で表示して手入力できる。公演地名・会場・住所・開始日・終了日を1セットとし、`公演地を追加`から東京公演・大阪公演等を複数登録する。空セットは保存しない。画像OCRで公演名が複数行に分かれた場合は結合候補と個別候補を表示し、会場ラベルと開催日も同じ公演地へ補完する。URL取込はOGP画像を公演ビジュアルとして使い、JSON-LDに会場と期間があれば同じ欄へ補完する。手入力済み項目は上書きしない。

### 進捗管理シートの長い公演名

2026-07-30以降、進捗管理シートは表示中の状態変更・参加日設定・詳細導線を操作数として数え、初期高を2個以下58%、3個64%、4個以上70%とする。本文上余白8pt・下余白12pt、公演名最大2行・88%までの縮小を使い、操作が少ない時は下部余白を詰め、長いタイトルと4操作の時は全操作を初期表示へ収める。既存の66%固定指定よりこの寸法を優先する。
